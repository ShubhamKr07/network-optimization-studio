# Weekly Product Insights — 2026-09-12

Source: `docs/product-insights/aggregates.json` (weekly usage aggregate counts only, no PII).

## Summary

There was little or no measurable product activity this week. Every aggregate in the
source data is empty or zero:

| Metric | Value |
|---|---|
| `funnelsByModel` | `{}` (no per-model funnel data) |
| `staleResolveRate` | `0` |
| `failuresByModel` | `{}` (no solve failures recorded) |
| `rejectionsByModel` | `{}` (no solve rejections recorded) |
| `runtimeP50` | `0` |
| `runtimeP95` | `0` |
| `cacheHitRate` | `0` |

## Funnel drop-offs

No data. `funnelsByModel` is empty, so no per-model funnel stages or drop-off points
can be identified this week.

## Solve failures / rejections

No data. `failuresByModel` and `rejectionsByModel` are both empty — no solve failures
or rejections were recorded for any model.

## Stale-without-resolve behavior

No data. `staleResolveRate` is `0`, which is indistinguishable here from "no scenarios
went stale" given the complete absence of other activity signals this week — there is
nothing to base a stale-resolution claim on.

## Solver performance

No data. `runtimeP50`/`runtimeP95` are both `0` and `cacheHitRate` is `0`, consistent
with no solves having run this week.

## Recommendations

Given the complete absence of activity signals, there is no basis in this data for any
product change recommendation this week. Recommended action:

1. **Continue collecting data** — re-run this report next week and confirm whether the
   empty aggregates reflect genuinely low usage (e.g. between-cohort lull) or an
   instrumentation/pipeline gap (e.g. the aggregation job not running, or events not
   reaching PostHog). If next week's aggregates are also all-empty, treat that as a
   signal to investigate the analytics pipeline itself rather than assuming zero usage.

No further recommendations are made, since making them would require inventing data
not present in the source aggregates.
