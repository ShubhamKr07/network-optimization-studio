Rectilinear, print-like button; primary is solid green-600, hover darkens one step.

```jsx
<Button>Run Optimizer</Button>
<Button variant="outline" size="sm">Cancel</Button>
<Button variant="destructive" size="sm">Delete</Button>
```

Variants: primary, secondary (pale green), outline, ghost, destructive, link. Sizes: default (36px), sm (30px), lg, icon (36x36).
