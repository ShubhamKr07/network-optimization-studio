Underline tabs — green rule under the active tab. For closable document tabs use studio/TabBar instead.

```jsx
<Tabs tabs={["Cost summary","Flows","Service stats"]} activeId="Flows" onChange={setTab} />
```
