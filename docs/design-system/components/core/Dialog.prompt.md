Modal on a dark scrim; serif title, sunken footer action strip.

```jsx
<Dialog open title="Run optimizer?" description="p = 4, uniform capacity 60,000." onClose={close}
  footer={<><Button variant="outline" size="sm">Cancel</Button><Button size="sm">Run</Button></>} />
```
