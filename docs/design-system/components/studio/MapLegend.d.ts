export interface MapLegendProps {
  /** Reference demand values for the bubble-size row. */
  demandRefs?: number[];
  /** Position it yourself (usually absolute bottom-left over the map). */
  style?: React.CSSProperties;
}
