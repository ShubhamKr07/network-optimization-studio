Static map-overlay legend: warehouse status triangles (outline/filled/dashed) + demand reference bubbles. Non-interactive (pointer-events: none).

```jsx
<MapLegend style={{ position: "absolute", bottom: 16, left: 16 }} />
```
