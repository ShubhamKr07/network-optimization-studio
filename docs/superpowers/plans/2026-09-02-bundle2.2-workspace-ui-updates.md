# Bundle 2.2 — Workspace UI Updates — Implementation Plan

> **For agentic workers:** Execution model is the **agent team** (role dispatch: `frontend-engineer`, `backend-engineer`, `qa-sdet`), in the shared worktree `/Users/shubhamkr/network-optimization-studio/.claude/worktrees/scn-v0.3-phase3.1` (branch `worktree-scn-v0.3-phase3.1`). Controller integrates + gates + merges to local `main`. Steps use `- [ ]` checkboxes.

**Goal:** Ship the 15 Bundle 2.2 UI/UX updates (spec `docs/superpowers/specs/2026-09-02-bundle2.2-workspace-ui-updates-design.md`, rev 2).

**Architecture:** Frontend-heavy React/Vite Workspace changes + two backend touches (A3 added-customer exclusion end-to-end; B3 model-scoped immutable reference-distances endpoint). Contract-first for B3 only.

**Tech Stack:** React + Vite + Tailwind + react-leaflet + wouter + TanStack Query; Express 5 + Drizzle; PuLP/CBC (untouched this bundle).

## Global Constraints (verbatim from spec)

- **DD-1:** base dataset files under `solvers/*/dataset/*.json` are never mutated. Reference distances are read-only; overrides live only in scenario `inputs`.
- **No modelId hardcoding for capability gates.** Use manifest `capabilities` / scenario config, never `modelId === "..."`.
- **Never edit generated code** (`lib/api-zod/src/generated/`, `lib/api-client-react/src/generated/`). Contract change = edit `openapi.yaml`, re-run Orval (`pnpm --filter @workspace/api-spec run codegen`), commit spec + regen together.
- Ownership/anti-enumeration unchanged (404 never 403). `solve.py` untouched.
- Distance unit is model-driven (`manifest.distanceUnit` via `useListModels()`), never hardcoded "mi".
- **Snapshot invariant:** every OUTPUT surface reads `displayedInputs`/`displayedResult` (the snapshot that produced the shown solution), never `localInputs`.
- **A2 fixed OFF radius = `FIXED_CUSTOMER_RADIUS = 6` px. `FOOTER_H = 24px`.**

## Serialization & Waves (agent-team, shared worktree = last-write-wins per file)

- **`Workspace.tsx`** is touched by C2 (header), C1 (footer viewport), B4 (tab order), B2 (sidebar gate), A3 (effective-map projection), possibly B6 (inputs-snapshot prop). **All `Workspace.tsx` edits are consolidated into ONE task (T9) with a single writer** — do NOT dispatch concurrent `Workspace.tsx` writers.
- **`openapi.yaml`** single writer = T2 (B3) only. A3 does not touch it.
- **Wave 1 (parallel, file-disjoint):** T1, T2, T3, T4, T5, T6, T8, T10a.
- **Wave 1.5 (gated on T2):** T7 (DistancesTab needs B3's generated hook + capability).
- **Wave 2 (after Wave 1 leaves land):** T9 (`Workspace.tsx` omnibus, single writer) + T10b (footer mounts in AppShell/Login/Register — disjoint from `Workspace.tsx`, may run alongside T9).
- **Wave 3:** T11 qa gate + live verify.
- Controller re-runs each task's gate on the merged state; ledger in `.superpowers/sdd/bundle2.2-progress.md` (git-ignored — never `git add`).

---

## Wave 1

### T1 — A3 backend: added-customer exclusion end-to-end (p-median-us + two-echelon)

**Role:** backend-engineer. **No OpenAPI/codegen** (`Scenario.inputs` is opaque; these are hand-written api-server Zod validators).

**Files:**
- Modify: `artifacts/api-server/src/validation/inputs/pMedian.ts` (`addedCustomerSchema` L39-56)
- Modify: `artifacts/api-server/src/validation/inputs/twoEchelon.ts` (`addedCustomerSchema` L36-45)
- Modify: `artifacts/api-server/src/solver/pmedian.ts` (`buildPayload` — p-median block L74-76, two-echelon L48-49)
- Modify: `artifacts/api-server/src/services/precheck.ts` (`buildActivePMedianIds` L158, `buildActiveTwoEchelonIds` L357)
- Test: `artifacts/api-server/src/**/__tests__` (pmedian payload, precheck, validators)

**Interfaces — Produces (frontend T8/T9 consume):**
- `addedCustomerSchema` gains `status: z.enum(["active","excluded"]).default("active")` in **both** models.
- Solver payload `excludedCustomerIds` now = base `customerOverrides` excluded ∪ `addedCustomers` with `status === "excluded"` (both models).
- Precheck active-id builders exclude added customers whose `status === "excluded"`.

**Steps:**
- [ ] Test: added customer with `status:"excluded"` → appears in `buildPayload().excludedCustomerIds` (p-median-us + two-echelon); default/`active` → absent.
- [ ] Test: precheck no longer counts a `status:"excluded"` added customer as active (both models).
- [ ] Test: validator accepts `addedCustomers[].status` (active/excluded), defaults to `active` when omitted (back-compat for existing rows).
- [ ] Implement schema + payload + precheck changes to pass.
- [ ] Run: `pnpm --filter api-server test` (relevant files). `solve.py` untouched.
- [ ] Commit `[B2.2-T1] A3 backend: addedCustomers.status excluded end-to-end (us+2E)`.

**DoD:** excluded added customer is dropped from the solve AND not counted active by precheck, for p-median-us and two-echelon; existing rows without `status` default to active. Stations unaffected (no addedCustomers on transport).

---

### T2 — B3 backend: model-scoped immutable reference-distances endpoint + capability

**Role:** backend-engineer. **Sole `openapi.yaml` writer.**

**Files:**
- Modify: `lib/dataset-schema/src/index.ts` (`ManifestSchema.capabilities` L161-173 → add `supportsReferenceDistances: z.boolean().optional().default(false)`)
- Modify: `solvers/p-median-us/manifest.json` (capabilities → `supportsReferenceDistances: true`)
- Modify: `artifacts/api-server/src/registry/modelRegistry.ts` (`toPublic` L67-84 + `PublicModelInfo.capabilities` L59 → expose `supportsReferenceDistances`)
- Modify: `lib/api-spec/openapi.yaml` (`ModelInfo.capabilities` L663-681 → add `supportsReferenceDistances` required bool; new `getReferenceDistances` operation `GET /models/{id}/reference-distances`, response `ReferenceDistances { pairs: [{fromId,fromCode,toId,toCode,distance}], distanceUnit }`) + **regen** (`pnpm --filter @workspace/api-spec run codegen`)
- Create: `artifacts/api-server/src/data/referenceDistances.ts` (boot-time loader: parse `solvers/p-median-us/dataset/distances.json` ordinal keys `"w,c"`, map ordinal→id via `dataset.ts` `WAREHOUSES`/`CUSTOMERS` array order (ordinal N → index N−1), validate every ordinal resolves; build the complete pair list once)
- Create/Modify: reference-distances route — add to `artifacts/api-server/src/routes/dataset.ts` (same unauth, model-scoped router family) or a new `routes/referenceDistances.ts` mounted in `routes/index.ts` **before** the scenarios router (unauthenticated).
- Test: `artifacts/api-server/src/**/__tests__` (registry capability, loader ordinal mapping, route 200/422)

**Interfaces — Produces (T7 consumes):**
- `GET /models/:id/reference-distances` → `200 { pairs: {fromId,fromCode,toId,toCode,distance}[], distanceUnit }` with `Cache-Control: public, immutable` + `ETag`; **complete unfiltered** base×base matrix; `422` for an owned-but-unsupported model (capability false); unauthenticated (matches `/dataset`, `/models`).
- Generated hook `useGetReferenceDistances({ id })` + `ReferenceDistances` type.
- `ModelInfo.capabilities.supportsReferenceDistances` on the public contract.

**Steps:**
- [ ] Test: loader builds 5200 pairs for p-median-us; every pair's `fromCode`/`toCode` resolves (no unmapped ordinal); throws on a deliberately corrupted ordinal.
- [ ] Test: route returns 5200 pairs + `distanceUnit:"mi"` + immutable/ETag headers; a model with `supportsReferenceDistances:false` → 422.
- [ ] Test: registry `toPublic` exposes `supportsReferenceDistances` (p-median-us true).
- [ ] Implement schema/manifest/registry, openapi + regen, loader, route.
- [ ] Run: `pnpm --filter @workspace/api-spec run codegen` then `pnpm run typecheck` + `pnpm --filter api-server test` + dataset-schema test.
- [ ] Commit `[B2.2-T2] B3 backend: model-scoped immutable reference-distances + supportsReferenceDistances (spec+regen)`.

**DoD:** immutable model-level endpoint returns the complete base matrix; capability gated; codegen regenerated (not hand-edited). No dataset file mutated.

---

### T3 — A1+A2 Input Map: layer checkboxes (all 3 toolbars) + size-by-demand + legend follows

**Role:** frontend-engineer.

**Files:**
- Modify: `artifacts/studio/src/components/workspace/tabs/InputMapTab.tsx` (`ToggleChip` buttons L274-297; toolbars — p-median `618-635`, transport, two-echelon; toggle state `~424`)
- Modify: `artifacts/studio/src/components/workspace/map/MapLegend.tsx` (hide swatch groups per layer state)
- Modify: `artifacts/studio/src/components/workspace/map/EntityMarkers.tsx` (`makeQuintileRadius`/`customerIcon` ~110/160 → honor `sizeByDemand`)
- Test: `artifacts/studio/src/**/InputMapTab*.test.tsx` (recreate; old one was deleted), MapLegend/EntityMarkers tests

**Interfaces — Consumes:** none new. **Produces:** in-component state only.

**Applicability matrix (A1):**
| Model | Layer checkboxes | Show-inactive |
|---|---|---|
| p-median-us / brazil | Warehouses, Customers | yes |
| transport-coal | Mines, Stations | **no** |
| two-echelon | Refineries, Customers | yes |

**Steps:**
- [ ] Test: p-median toolbar renders shadcn `Checkbox`es (not buttons) for Warehouses/Customers/Show-inactive; place-pin stay buttons.
- [ ] Test: transport toolbar = Mines/Stations checkboxes, NO Show-inactive; two-echelon = Refineries/Customers + Show-inactive.
- [ ] Test: toggling Customers off hides customer/demand swatches in `MapLegend`; warehouses off hides facility swatches.
- [ ] Test: `sizeByDemand` checkbox default ON; OFF → every demand-bearing marker radius = `FIXED_CUSTOMER_RADIUS = 6` (covers p-median customers, two-echelon customers, **transport stations**); legend demand-bubble section hidden when OFF.
- [ ] Implement across all three toolbars; export `FIXED_CUSTOMER_RADIUS`.
- [ ] Run: `pnpm --filter studio test` (touched).
- [ ] Commit `[B2.2-T3] A1/A2 input-map checkboxes (all 3 toolbars) + size-by-demand + legend-follow`.

**DoD:** all input maps use checkboxes with per-model layer set; Show-inactive only where facility status exists; size-by-demand covers every demand-bearing role; legend visibility driven by live layer state.

---

### T4 — A4 Output Map: route hover tooltip (model-unit-aware)

**Role:** frontend-engineer.

**Files:**
- Modify: `artifacts/studio/src/components/NetworkMap.tsx` (route polylines L442-479 → add react-leaflet `<Tooltip>`; accept `distanceUnit` prop)
- Modify: `artifacts/studio/src/components/workspace/tabs/OutputMapTab.tsx` (thread `distanceUnit` from `useListModels()` L108 into `<NetworkMap>`)
- Test: `NetworkMap.test.tsx`

**Interfaces — Consumes:** `distanceUnit: string` prop. **Produces:** none.

**Steps:**
- [ ] Test: each route polyline has a translucent `pointer-events-none` Tooltip = `{fromCity} → {toCity}` + `{edge.distance} {distanceUnit}`.
- [ ] Test: a **non-`mi`** unit (e.g. `"km"`) renders in the tooltip (capability enforced, not accidental).
- [ ] Implement; resolve cities from edge `fromId`/`toId` via the effective dataset lookup already used by markers/popup.
- [ ] Run: `pnpm --filter studio test NetworkMap`.
- [ ] Commit `[B2.2-T4] A4 route hover tooltip (cities + model-unit distance)`.

**DoD:** hovering a route shows cities + length in the model's unit; click `CustomerPopup` unchanged.

---

### T5 — B5 Solution Summary: open-facility set per scenario (compare mode)

**Role:** frontend-engineer.

**Files:**
- Modify: `artifacts/studio/src/components/workspace/tabs/CostSummaryTab.tsx` (compare rows L191-303; insert after "Weighted avg. distance")
- Reuse: the existing `openFacilityIds()` helper (locate via grep — mine-leg-safe) + city lookup
- Test: `CostSummaryTab.test.tsx`

**Interfaces — Consumes:** each compare column's persisted `inputs` + `result`. **Produces:** none.

**Steps:**
- [ ] Test: compare mode shows an "Open facilities" row (after Weighted avg. distance) listing each scenario's open facilities **by city name**.
- [ ] Test: two-echelon column does NOT list the fixed mine (uses `openFacilityIds()`, not raw `edges[].fromId`).
- [ ] Test: row gated on `capabilities.supportsFacilityStatus` — absent for transport-coal, present for two-echelon (NOT `supportsP`).
- [ ] Test: an `aw-…` added facility resolves its city from that column's own persisted `inputs.addedWarehouses`/`addedRefineries` (base ∪ column inputs).
- [ ] Implement.
- [ ] Run: `pnpm --filter studio test CostSummary`.
- [ ] Commit `[B2.2-T5] B5 open-facility-set-by-city row in compare`.

**DoD:** correct per-model open-facility city list in compare; mine-safe; capability-gated; single mode unchanged.

---

### T6 — B1 + B6 output tabs: utilization gate + display-ID

**Role:** frontend-engineer.

**Files:**
- Modify: `artifacts/studio/src/components/workspace/tabs/OpenWarehousesTab.tsx` (utilization header L56-58 + cell L66; warehouseId render L64)
- Modify: `artifacts/studio/src/components/workspace/tabs/AssignmentsTab.tsx` (ids L47-48)
- Test: both tab tests

**Interfaces — Consumes:** `displayedInputs` snapshot (capacityMode + added-entity displayCodes). **If the tab does not already receive the inputs snapshot, T9 wires the prop** (note the required prop shape here for T9).

**Steps:**
- [ ] Test: Utilization column hidden when `displayedInputs.capacityMode === "none"`; shown for uniform/per_wh and two-echelon.
- [ ] Test: a user-created warehouse (`aw-…`) renders its **display ID** (not uid) in Open Warehouses + Customer Assignments, resolved from `displayedInputs` (not `localInputs`).
- [ ] Test: an unsaved capacityMode/rename edit in `localInputs` does NOT change the displayed column/label (snapshot invariant).
- [ ] Implement `displayCodeById` from `displayedInputs` (mirror `DistancesTab`'s pattern).
- [ ] Run: `pnpm --filter studio test` (tabs).
- [ ] Commit `[B2.2-T6] B1 util gate (capacityMode=none) + B6 display-ID for added warehouses`.

**DoD:** utilization hidden at capacityMode=none; added warehouses show human ID from the snapshot in both tabs.

---

### T8 — A3 frontend dialogs: customer Active/Excluded (role-gated)

**Role:** frontend-engineer. Gated on **T1** interface (`addedCustomers[].status`).

**Files:**
- Modify: `artifacts/studio/src/components/workspace/map/types.ts` (`AddedCustomerInput` L20-28 → add `status?: "active" | "excluded"`; confirm `EntityRoleConfig`)
- Modify: `artifacts/studio/src/components/workspace/map/dialogs/EditCustomerDialog.tsx` (add status control; emit `status` in patch)
- Modify: `artifacts/studio/src/components/workspace/map/dialogs/CreateEntityDialog.tsx` (`kind==="cs"` branch L134-144 → status control **only for the customer role, NOT stations**; `STATION_ROLE.hasStatus === false` gates it out)
- Test: both dialog tests

**Interfaces — Consumes:** T1's `status` field. **Produces:** dialogs emit `status`; T9 projects it.

**Steps:**
- [ ] Test: `EditCustomerDialog` shows Active/Excluded control, emits `{demand, status}`.
- [ ] Test: `CreateEntityDialog` customer branch shows the status control; **station role does NOT** (gate on the role config, e.g. `role.hasStatus`/customer-role flag, not `kind` alone).
- [ ] Implement; add `status` to `AddedCustomerInput`.
- [ ] Run: `pnpm --filter studio test` (dialogs).
- [ ] Commit `[B2.2-T8] A3 frontend: customer Active/Excluded in Edit+Create dialogs (stations gated out)`.

**DoD:** both dialogs set customer status; transport stations never receive it.

---

### T10a — AppFooter component

**Role:** frontend-engineer. Leaf (no shell edits here).

**Files:**
- Create: `artifacts/studio/src/components/AppFooter.tsx` (bottom-center `© Developed by hx1`; export `FOOTER_H = 24`)
- Test: `AppFooter.test.tsx`

**Steps:**
- [ ] Test: renders `©` + `Developed by hx1`, centered, height `FOOTER_H`.
- [ ] Implement.
- [ ] Commit `[B2.2-T10a] AppFooter component`.

---

## Wave 1.5 (gated on T2)

### T7 — B3 frontend: DistancesTab read-only reference table + client-side view filter

**Role:** frontend-engineer. Gated on **T2** (hook + capability). Disjoint from `Workspace.tsx`.

**Files:**
- Modify: `artifacts/studio/src/components/workspace/tabs/DistancesTab.tsx` (add reference section above the editable override grid)
- Test: `DistancesTab.test.tsx`

**Interfaces — Consumes:** `useGetReferenceDistances({id: modelId})`, `capabilities.supportsReferenceDistances`, live `localInputs`.

**Steps:**
- [ ] Test: reference section renders only when `supportsReferenceDistances` (capability, not modelId); hidden otherwise.
- [ ] Test: **client-side filter** against `localInputs` — default 5200 rows shown → one base WH inactive → 5000 → one base customer excluded → 5174 → both → 4975 (instant, no refetch).
- [ ] Test: added entities absent from the reference section; a base×base `distanceOverrides` entry does NOT change the reference baseline value.
- [ ] Test: rows are virtualized (assert not all 5200 DOM nodes mount).
- [ ] Implement infinite-scroll-free bulk fetch + row virtualization + `localInputs`-derived filter.
- [ ] Run: `pnpm --filter studio test Distances`.
- [ ] Commit `[B2.2-T7] B3 frontend: reference-distances table + client-side view filter`.

**DoD:** immutable matrix fetched once, filtered live against unsaved status edits; virtualized; capability-gated.

---

## Wave 2

### T9 — `Workspace.tsx` omnibus (SINGLE WRITER)

**Role:** frontend-engineer. Runs after Wave 1 leaves. Consolidates every `Workspace.tsx` edit. Gated on: T1 (status), T6 (tab prop shape), T10a (AppFooter).

**Files:**
- Modify: `artifacts/studio/src/pages/Workspace.tsx` only.

**Sub-changes (all in one task/commit sequence):**
1. **C2 header (items 13/14/15):** restructure header (L2189-2256) into `grid-cols-[auto_1fr_auto]`. Left: back-arrow + Scenario `<select>` (`select-scenario-context` moved from L2206-2223, testid preserved). Center (`1fr min-w-0`): chapter+summary (`workspace-chapter-summary`) — drop `max-w-[38%] truncate` + absolute positioning, full text, centered in track. Right: email + Logout (moved from L2244-2255). Remove title/subtitle block L2202-2205 (`text-app-name` goes away). Result-history stepper + "Save as scenario" + Run-Optimizer cluster stay in their current relative position (do not reposition).
2. **C1 footer viewport:** mount `<AppFooter/>` inside the `h-screen flex flex-col` column (root L2188, inside `.scn-theme`); reserve `FOOTER_H` so the body (`flex-1` L2310) doesn't overlap map content.
3. **B4 tab order:** reorder `OUTPUT_ENTRIES` (L732-739) → Output Map, **Solution Summary (`cost-summary`)**, Open Warehouses, Customer Assignments, [Flows], Service Stats.
4. **B2 sidebar gate:** gate the output entries passed to `<SidebarTree outputs=...>` (L2317) by `activeModelManifest?.capabilities.outputGrids` (reuse the `entityToCapability` map at L2135-2141) → Flows disappears from the p-median-us sidebar. Content gate L2142-2148 stays as defense-in-depth.
5. **A3 effective-map projection:** `pmedianMapCustomers` L356 — derive `excluded` from the added customer's `status` instead of hardcoded `false`; apply the same to the two-echelon added-customer projection (locate its analogue; transport stations keep `false` — no excluded concept).
6. **B6 prop (only if needed):** if `OpenWarehousesTab`/`AssignmentsTab` don't already receive the `displayedInputs` snapshot, pass it at their render sites (L2150/L2151 region).

**Steps:**
- [ ] Tests (add to `Workspace.test.tsx` / model-specific): header grid has selector-left, centered untruncated summary, email/logout top-right, stepper+Save unmoved; footer present inside viewport; tab order (Solution Summary 2nd); Flows absent for p-median-us sidebar, present for a flows model; added excluded customer projects `excluded:true`.
- [ ] Implement all six sub-changes.
- [ ] Run: `pnpm run typecheck && pnpm --filter studio test`.
- [ ] Commit `[B2.2-T9] Workspace omnibus: C2 header, C1 footer, B4 order, B2 sidebar gate, A3 projection, B6 prop`.

**DoD:** header/footer/tab-order/sidebar-gate/projection all correct; preserved testids (`select-scenario-context`, `workspace-chapter-summary`); no `Workspace.tsx` edits elsewhere.

---

### T10b — Footer mounts in remaining shells

**Role:** frontend-engineer. Disjoint from `Workspace.tsx` (may run alongside T9). Gated on T10a.

**Files:**
- Modify: `artifacts/studio/src/components/AppShell.tsx` (root `h-screen flex flex-col` L32-33 → mount `<AppFooter/>`, reserve `FOOTER_H`)
- Modify: `artifacts/studio/src/pages/Login.tsx` (L40) + `artifacts/studio/src/pages/Register.tsx` (L44) → mount `<AppFooter/>`
- Test: `AppShell` / auth-page render tests assert footer present + narrow-screen no-overlap

**Steps:**
- [ ] Test: footer renders on AppShell-wrapped pages (Landing/Studio), Login, Register; narrow viewport (e.g. 375px) → no map/footer overlap on a Workspace-height assertion (covered in T9) and no clipping on AppShell.
- [ ] Implement mounts, reserving `FOOTER_H` in each `h-screen` column.
- [ ] Commit `[B2.2-T10b] mount AppFooter in AppShell/Login/Register`.

**DoD:** `© Developed by hx1` on every current route (Landing, Login, Register, Workspace, Studio); Compare excluded (route removed).

---

## Wave 3

### T11 — QA gate + accuracy + live verify

**Role:** qa-sdet.

**Steps:**
- [ ] Full repository gate:
  ```bash
  pnpm run typecheck && pnpm --filter api-server test && pnpm --filter studio test \
    && (cd artifacts/api-server/src/solver && python3 -m pytest tests/ -x)
  ```
- [ ] A3 touches the merge/solver **payload boundary** → run `python3 e2e_accuracy.py` and `python3 e2e_journey.py` (from `artifacts/api-server/src/solver/tests/`) **unmodified**; confirm no accuracy regression (`solve.py` itself unchanged).
- [ ] Live verify (claude-in-chrome, local dev, disposable account, purge after): footer on all pages; header layout (selector left / summary centered untruncated / email+logout right / stepper+Save unmoved); input-map checkboxes + size-by-demand + legend-follow; customer Active/Excluded in both dialogs (station has none); route hover tooltip; utilization hidden at capacityMode=none; Flows gone from p-median-us sidebar; Distances reference table + live filter; Solution Summary 2nd + compare open-facility-by-city row; added-warehouse display ID in Open Warehouses/Assignments.
- [ ] Ledger + CLAUDE.md update; controller merges to local `main`.

**DoD:** full gate green; `e2e_accuracy.py`/`e2e_journey.py` unmodified & passing; all 15 items verified live.

---

## Self-Review (author checklist)

- **Spec coverage:** items 1(C1/T10a+T10b+T9-footer), 2/3(A1/T3), 4(A2/T3), 5(B1/T6), 6(B2/T9), 7(B3/T2+T7), 8(B4/T9), 9(B5/T5), 10(A3/T1+T8+T9-projection), 11(B6/T6), 12(A4/T4), 13/14/15(C2/T9). All mapped.
- **Types:** `addedCustomers[].status` defined T1, consumed T8/T9; `FIXED_CUSTOMER_RADIUS` T3; `supportsReferenceDistances` T2, consumed T7; `useGetReferenceDistances`/`ReferenceDistances` T2→T7; `FOOTER_H` T10a→T9/T10b; `openFacilityIds()` reused T5.
- **Serialization:** single `Workspace.tsx` writer (T9); single `openapi.yaml` writer (T2). No concurrent shared-file writers.
