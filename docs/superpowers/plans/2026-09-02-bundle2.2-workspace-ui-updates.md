# Bundle 2.2 — Workspace UI Updates — Implementation Plan

> **For agentic workers:** Execution model is the **agent team** (role dispatch: `frontend-engineer`, `backend-engineer`, `qa-sdet`), in the shared worktree `/Users/shubhamkr/network-optimization-studio/.claude/worktrees/scn-v0.3-phase3.1` (branch `worktree-scn-v0.3-phase3.1`). Controller integrates + gates + merges to local `main`. Steps use `- [ ]` checkboxes.

**Goal:** Ship the 15 Bundle 2.2 UI/UX updates (spec `docs/superpowers/specs/2026-09-02-bundle2.2-workspace-ui-updates-design.md`, rev 2).

**Architecture:** Frontend-heavy React/Vite Workspace changes + two backend touches (A3 added-customer exclusion end-to-end; B3 model-scoped immutable reference-distances endpoint). Contract-first for B3 only.

**Tech Stack:** React + Vite + Tailwind + react-leaflet + wouter + TanStack Query; Express 5 + Drizzle; PuLP/CBC (untouched this bundle).

## Global Constraints (verbatim from spec)

- **DD-1:** base dataset files under `solvers/*/dataset/*.json` are never mutated. Reference distances are read-only; overrides live only in scenario `inputs`.
- **No modelId hardcoding for capability gates.** Use manifest `capabilities` / scenario config, never `modelId === "..."`.
- **Never edit generated code** (`lib/api-zod/src/generated/`, `lib/api-client-react/src/generated/`). Contract change = edit `openapi.yaml`, re-run Orval (`pnpm --filter @workspace/api-spec run codegen`), commit spec + regen together.
- Ownership/anti-enumeration unchanged (404 never 403). `solve.py` untouched.
- Distance unit is model-driven (`manifest.distanceUnit` via `useListModels()`), never hardcoded "mi".
- **Snapshot invariant:** every OUTPUT surface reads `displayedInputs`/`displayedResult` (the snapshot that produced the shown solution), never `localInputs`.
- **A2 fixed OFF radius = `FIXED_CUSTOMER_RADIUS = 6` px. `FOOTER_H = 24px`.**

## Serialization & Waves (agent-team, shared worktree = last-write-wins per file)

Shared-file writers (single-writer rule):
- **`solvers/*/manifest.json` + `lib/dataset-schema` `ManifestSchema`** → **T0 only** (solver-owned seam, per AGENTS.md; done by `solver-engineer`). Removing manifest edits from T1/T2 is what makes them file-disjoint.
- **`Workspace.tsx`** (C2 header, C1 footer viewport, B4 tab order, B2 sidebar gate, A3 projection, **plus B6/B1 tab call-site props and B3 DistancesTab call-site props**) → **T9 only**. No concurrent `Workspace.tsx` writers.
- **`InputMapTab.tsx`** → T3 (checkboxes/size/legend) **and** T8 (customer-status patch wiring) → **serialize T3 → T8**.
- **`openapi.yaml`** → **T2 only**. A3 does not touch it (`Scenario.inputs` opaque).
- **`MapLegend.tsx` / `EntityMarkers.tsx`** → T3 only.

Per-commit-green rule for atomic call-site wiring: T6 and T7 add their new props **as optional with safe defaults** so each component commit typechecks standalone; **T9 supplies the real props at the call sites** (single `Workspace.tsx` writer). The feature is live once T9 lands.

Waves:
- **Wave 0:** T0 (manifests + `ManifestSchema` + tests). Lands first — T1, T2 depend on it.
- **Wave 1 (parallel, file-disjoint, after T0):** T1, T2, T3, T4, T5, T6, T10a.
- **Wave 1.5:** T7 (gated on T2: hook + capability), T8 (gated on **T1** status field **and** serialized after **T3** — shares `InputMapTab.tsx`).
- **Wave 2 (after Wave 1/1.5 leaves land):** T9 (`Workspace.tsx` omnibus incl. all call-site wiring, single writer) + T10b (footer mounts in AppShell/Login/Register — disjoint from `Workspace.tsx`).
- **Wave 3:** T11 qa gate + accuracy + live verify.
- Controller re-runs each task's gate on the merged state; ledger in `.superpowers/sdd/bundle2.2-progress.md` (git-ignored — never `git add`).

---

## Wave 0

### T0 — Manifest + ManifestSchema seam (solver-owned, single writer)

**Role:** solver-engineer. Lands FIRST. Owns every `manifest.json` + `ManifestSchema` edit so T1/T2 stay file-disjoint (per AGENTS.md the manifest is the solver-owned seam — coordinate here, not as an unannounced backend edit).

**Files:**
- Modify: `solvers/p-median-us/manifest.json` — `inputsSchema.addedCustomers` gains `status` (Active/Excluded, optional/back-compat default active); `capabilities.supportsReferenceDistances: true`; `capabilities.supportsAddedCustomerExclusion: true`
- Modify: `solvers/two-echelon-gold-au/manifest.json` — `inputsSchema.addedCustomers` gains `status` (same); `capabilities.supportsAddedCustomerExclusion: true`
- Modify: `solvers/p-median-brazil/manifest.json` — `capabilities.supportsAddedCustomerExclusion: false` (**explicit** — Brazil's solver applies no customer exclusion; the shared p-median UI/precheck/payload must NOT let an added customer be excluded there; base-customer status is still allowed)
- Modify: `lib/dataset-schema/src/index.ts` — `ManifestSchema.capabilities` (L161-173) gains `supportsReferenceDistances: z.boolean().optional().default(false)` **and** `supportsAddedCustomerExclusion: z.boolean().optional().default(false)`
- Test: `lib/dataset-schema/src/*.test.ts` (both capability parse/default; all edited manifests validate)

**Interfaces — Produces:** `ManifestSchema.capabilities.supportsReferenceDistances` + `.supportsAddedCustomerExclusion`; manifests advertise `addedCustomers[].status`; p-median-us `supportsReferenceDistances:true`; us+2E `supportsAddedCustomerExclusion:true`, brazil `false`. Consumed by T1 (payload/precheck gate), T2 (registry/openapi), T7 (ref capability), T8/T9 (added-exclusion capability).

**Steps:**
- [ ] Test: `ManifestSchema` accepts + defaults both new capabilities (false when omitted); p-median-us ref=true, us+2E added-excl=true, brazil added-excl=false; all edited manifests validate; `addedCustomers` schema description carries `status`.
- [ ] Implement the three manifest edits + the two `ManifestSchema` fields.
- [ ] Run: `pnpm --filter @workspace/dataset-schema test` + `pnpm run typecheck`.
- [ ] Commit `[B2.2-T0] manifests: addedCustomers.status + supportsReferenceDistances + supportsAddedCustomerExclusion + ManifestSchema`.

**DoD:** manifests + `ManifestSchema` carry the new fields with back-compat defaults; Brazil explicitly opts out of added-customer exclusion; no other file touched.

---

## Wave 1

### T1 — A3 backend: added-customer exclusion end-to-end (p-median-us + two-echelon)

**Role:** backend-engineer. **No OpenAPI/codegen, no manifest** (`Scenario.inputs` is opaque; validators are hand-written api-server Zod; manifests handled in T0). Depends on T0.

**Files:**
- Modify: `artifacts/api-server/src/validation/inputs/pMedian.ts` (`addedCustomerSchema` L39-56)
- Modify: `artifacts/api-server/src/validation/inputs/twoEchelon.ts` (`addedCustomerSchema` L36-45)
- Modify: `artifacts/api-server/src/solver/pmedian.ts` (`buildPayload` — p-median block L74-76, two-echelon L48-49)
- Modify: `artifacts/api-server/src/services/precheck.ts` (`buildActivePMedianIds` L158, `buildActiveTwoEchelonIds` L357)
- Test: `artifacts/api-server/src/**/__tests__` (pmedian payload, precheck, validators)

**Model gate (review):** the p-median schema/`buildPayload`/`buildActivePMedianIds` are **shared by p-median-us AND p-median-brazil**. Added-customer exclusion must be gated on `capabilities.supportsAddedCustomerExclusion` (us true, brazil false) so Brazil's UI/precheck/payload stay consistent with its solver (which ignores customer exclusion). The registry/manifest capability is available server-side via the model registry.

**Interfaces — Produces (frontend T8/T9 consume):**
- `addedCustomerSchema` gains `status: z.enum(["active","excluded"]).default("active")` in **both** p-median and two-echelon (schema accepts it regardless of model; the *effect* is capability-gated).
- Solver payload `excludedCustomerIds` = base `customerOverrides` excluded ∪ (`addedCustomers` with `status==="excluded"` **only when `supportsAddedCustomerExclusion`**). Base-customer exclusion is unchanged for all p-median models.
- Precheck active-id builders exclude added customers whose `status==="excluded"` **only when the model supports added-customer exclusion**.

**Steps:**
- [ ] Test: for p-median-**us** and two-echelon, an added customer `status:"excluded"` → appears in `buildPayload().excludedCustomerIds`; default/`active` → absent.
- [ ] Test (**Brazil-negative**): for p-median-**brazil**, an added customer `status:"excluded"` is **NOT** added to `excludedCustomerIds` (capability false), and precheck **still counts it active**. Base-customer `customerOverrides` exclusion behaves the same for Brazil as before.
- [ ] Test: precheck skips a `status:"excluded"` added customer as active only when supported (us/2E yes, brazil no).
- [ ] Test: validator accepts `addedCustomers[].status`, defaults `active` when omitted (back-compat).
- [ ] Implement schema + capability-gated payload + capability-gated precheck.
- [ ] Run: `pnpm --filter api-server test`. `solve.py` untouched.
- [ ] Commit `[B2.2-T1] A3 backend: addedCustomers.status exclusion, capability-gated (us+2E on, brazil off)`.

**DoD:** excluded added customer is dropped from the solve AND not counted active by precheck for us + two-echelon; Brazil added customers can never be excluded (payload + precheck agree with its solver); existing rows default active; stations unaffected.

---

### T2 — B3 backend: model-scoped reference-distances endpoint (explicit ETag revalidation)

**Role:** backend-engineer. **Sole `openapi.yaml` writer.** Depends on T0 (`ManifestSchema.supportsReferenceDistances` + p-median-us manifest capability). **Does NOT touch any manifest or `ManifestSchema`** (T0 owns those).

**Files:**
- Modify: `artifacts/api-server/src/registry/modelRegistry.ts` (`toPublic` L67-84 + `PublicModelInfo.capabilities` L59 → expose **both** `supportsReferenceDistances` and `supportsAddedCustomerExclusion`)
- Modify: `lib/api-spec/openapi.yaml` (`ModelInfo.capabilities` L663-681 → add `supportsReferenceDistances` **and** `supportsAddedCustomerExclusion` required bools; new `getReferenceDistances` operation `GET /models/{id}/reference-distances` with explicit responses: `200 ReferenceDistances { pairs: [{fromId,fromCode,toId,toCode,distance}], distanceUnit }`, **`304` (no content, `If-None-Match` match)**, **`422` (known-but-unsupported model** — ownerless endpoint, not "owned")) + **regen** (`pnpm --filter @workspace/api-spec run codegen`)
- Create: `artifacts/api-server/src/data/referenceDistances.ts` (boot-time loader: parse `solvers/p-median-us/dataset/distances.json` ordinal keys `"w,c"`, map ordinal→id via `dataset.ts` `WAREHOUSES`/`CUSTOMERS` array order (ordinal N → index N−1), validate every ordinal resolves; build the complete pair list once; compute a stable **content ETag from the dataset `version.json`/hash** — `lib/dataset-schema` already exposes a hash/version helper)
- Create/Modify: reference-distances route — new `routes/referenceDistances.ts` mounted in `routes/index.ts` **before** the scenarios router (unauthenticated, model-scoped).
- Test: `artifacts/api-server/src/**/__tests__` (registry capability, loader ordinal mapping, route 200/422/304)

**Interfaces — Produces (T7 consumes):**
- `GET /models/:id/reference-distances` → `200 { pairs: {fromId,fromCode,toId,toCode,distance}[], distanceUnit }`; **complete unfiltered** base×base matrix; `422` for an owned-but-unsupported model (capability false); unauthenticated (matches `/dataset`, `/models`).
- **Caching:** the app sets `app.set("etag", false)` globally, so the route sets its **own explicit `ETag`** (derived from the dataset version/hash) and `Cache-Control: public, max-age=0, must-revalidate`; honors `If-None-Match` → `304`. (Unversioned stable URL + ETag revalidation, per review — avoids serving stale data after any future dataset change while still skipping re-download when unchanged.)
- Generated hook — use the **generated path-parameter signature `useGetReferenceDistances(modelId, options)`** (confirm the exact generated shape after regen) + `ReferenceDistances` type.
- `ModelInfo.capabilities.supportsReferenceDistances` **and** `.supportsAddedCustomerExclusion` on the public contract.

**Steps:**
- [ ] Test: loader builds 5200 pairs for p-median-us; every pair's `fromCode`/`toCode` resolves (no unmapped ordinal); throws on a deliberately corrupted ordinal.
- [ ] Test: route returns 5200 pairs + `distanceUnit:"mi"`; sets an explicit `ETag` + `Cache-Control: public, max-age=0, must-revalidate`; a repeat request with matching `If-None-Match` → **304 no-content**; a **known-but-unsupported** model (`supportsReferenceDistances:false`) → **422**.
- [ ] Test: registry `toPublic` exposes both `supportsReferenceDistances` (p-median-us true) and `supportsAddedCustomerExclusion` (us/2E true, brazil false); generated-client test covers the 304 + 422 responses.
- [ ] Implement registry, openapi (200/304/422) + regen, loader (+ ETag), route.
- [ ] Run: `pnpm --filter @workspace/api-spec run codegen` then `pnpm run typecheck` + `pnpm --filter api-server test`.
- [ ] Commit `[B2.2-T2] B3 backend: reference-distances endpoint (200/304/422) + explicit ETag + capabilities (spec+regen)`.

**DoD:** endpoint returns the complete base matrix with explicit ETag + `304` revalidation + `422` for known-unsupported; both new capabilities on the public contract; codegen regenerated (200/304/422 documented, not hand-edited). No dataset file mutated.

---

### T3 — A1+A2 Input Map: layer checkboxes (all 3 toolbars) + size-by-demand + legend follows

**Role:** frontend-engineer.

**Files:**
- Modify: `artifacts/studio/src/components/workspace/tabs/InputMapTab.tsx` (`ToggleChip` buttons L274-297; toolbars — p-median `618-635`, transport, two-echelon; toggle state `~424`)
- Modify: `artifacts/studio/src/components/workspace/map/MapLegend.tsx` (hide swatch groups per layer state)
- Modify: `artifacts/studio/src/components/workspace/map/EntityMarkers.tsx` (`makeQuintileRadius`/`customerIcon` ~110/160 → honor `sizeByDemand`)
- Test: `artifacts/studio/src/**/InputMapTab*.test.tsx` (recreate; old one was deleted), MapLegend/EntityMarkers tests

**Interfaces — Consumes:** none new. **Produces:** in-component state only.

**Applicability matrix (A1):**
| Model | Layer checkboxes | Show-inactive |
|---|---|---|
| p-median-us / brazil | Warehouses, Customers | yes |
| transport-coal | Mines, Stations | **no** |
| two-echelon | Refineries, Customers | yes |

**Steps:**
- [ ] Test: p-median toolbar renders shadcn `Checkbox`es (not buttons) for Warehouses/Customers/Show-inactive; place-pin stay buttons.
- [ ] Test: transport toolbar = Mines/Stations checkboxes, NO Show-inactive; two-echelon = Refineries/Customers + Show-inactive.
- [ ] Test: toggling Customers off hides customer/demand swatches in `MapLegend`; warehouses off hides facility swatches.
- [ ] Test: `sizeByDemand` checkbox default ON; OFF → every demand-bearing marker radius = `FIXED_CUSTOMER_RADIUS = 6` (covers p-median customers, two-echelon customers, **transport stations**); legend demand-bubble section hidden when OFF.
- [ ] Test (**two-echelon fixed mine**): the mine marker is gated by the Refineries/warehouse layer toggle — disabling **Refineries** hides both refinery markers **and** the fixed mine (the mine is currently rendered outside `EntityMarkers`, so it must be brought under the same layer gate; spec puts the mine implicit under the Refineries layer).
- [ ] Implement across all three toolbars; export `FIXED_CUSTOMER_RADIUS`; put the mine marker under the refinery layer toggle.
- [ ] Run: `pnpm --filter studio test` (touched).
- [ ] Commit `[B2.2-T3] A1/A2 input-map checkboxes (all 3 toolbars) + size-by-demand + legend-follow + mine under refinery layer`.

**DoD:** all input maps use checkboxes with per-model layer set; Show-inactive only where facility status exists; size-by-demand covers every demand-bearing role; the fixed mine hides with the Refineries layer; legend visibility driven by live layer state.

---

### T4 — A4 Output Map: route hover tooltip (model-unit-aware)

**Role:** frontend-engineer.

**Files:**
- Modify: `artifacts/studio/src/components/NetworkMap.tsx` (route polylines L442-479 → add react-leaflet `<Tooltip>`; accept `distanceUnit` prop)
- Modify: `artifacts/studio/src/components/workspace/tabs/OutputMapTab.tsx` (thread `distanceUnit` from `useListModels()` L108 into `<NetworkMap>`)
- Test: `NetworkMap.test.tsx`

**Interfaces — Consumes:** `distanceUnit: string` prop. **Produces:** none.

**Steps:**
- [ ] Test: each route polyline has a translucent `pointer-events-none` Tooltip = `{fromCity} → {toCity}` + `{edge.distance} {distanceUnit}`.
- [ ] Test: a **non-`mi`** unit (e.g. `"km"`) renders in the tooltip (capability enforced, not accidental).
- [ ] Implement; resolve cities from edge `fromId`/`toId` via the effective dataset lookup already used by markers/popup.
- [ ] Run: `pnpm --filter studio test NetworkMap`.
- [ ] Commit `[B2.2-T4] A4 route hover tooltip (cities + model-unit distance)`.

**DoD:** hovering a route shows cities + length in the model's unit; click `CustomerPopup` unchanged.

---

### T5 — B5 Solution Summary: open-facility set per scenario (compare mode)

**Role:** frontend-engineer.

**Files:**
- Modify: `artifacts/studio/src/components/workspace/tabs/CostSummaryTab.tsx` (compare rows L191-303; insert after "Weighted avg. distance")
- Reuse: the existing `openFacilityIds()` helper (locate via grep — mine-leg-safe) + city lookup
- Test: `CostSummaryTab.test.tsx`

**Interfaces — Consumes:** each compare column's persisted `inputs` + `result`. **City-name source (review):** `CostSummaryTab` has no dataset today (only `useListModels`). Add `useGetDataset({ modelId })` inside the tab (compare is single-model, so one fetch) to resolve **base** facility id → city; **added** facilities resolve from each column's own persisted `inputs.addedWarehouses`/`inputs.addedRefineries`. Define loading behavior (render the row's cities once the dataset resolves; before that, fall back to the facility id/code) and the **unknown-id fallback** (render the raw id/code, never blank). **Produces:** none.

**Existing state (review):** `CostSummaryTab` compare already renders an **Open facilities** count row and an **Aggregate utilization** row together inside one `supportsP` fragment. Do not duplicate.

**Steps:**
- [ ] Test: the new **city-list** row appears right after "Weighted avg. distance", listing each scenario's open facilities **by city name**; the pre-existing Open facilities **count** row is **replaced/moved** (not duplicated alongside the city list).
- [ ] Test: new city-list row gated **independently** on `capabilities.supportsFacilityStatus` (present for two-echelon, absent for transport-coal); NOT `supportsP`.
- [ ] Test: **Aggregate utilization** row stays under its existing `supportsP` applicability rule (unchanged).
- [ ] Test: two-echelon gets **exactly one** city-list row and does **NOT** acquire an Aggregate utilization row.
- [ ] Test: two-echelon column does NOT list the fixed mine (uses `openFacilityIds()`, not raw `edges[].fromId`).
- [ ] Test: a **base** facility resolves to its dataset city name (via `useGetDataset`); an **added** `aw-…` facility resolves its city from that column's own persisted inputs — cover **both** storage keys: `inputs.addedWarehouses` (p-median) and `inputs.addedRefineries` (two-echelon).
- [ ] Test: an unknown/unresolvable facility id falls back to the raw id/code (never blank); pre-dataset-load renders the fallback.
- [ ] Implement (`useGetDataset({ modelId })` + base∪added city map).
- [ ] Run: `pnpm --filter studio test CostSummary`.
- [ ] Commit `[B2.2-T5] B5 open-facility-set-by-city row (replaces count row; supportsFacilityStatus gate)`.

**DoD:** one city-list row per model, replacing the old count row; mine-safe; gated on `supportsFacilityStatus`; Aggregate utilization untouched under `supportsP`; both added-facility storage keys covered; single mode unchanged.

---

### T6 — B1 + B6 output tabs: utilization gate + display-ID

**Role:** frontend-engineer.

**Files:**
- Modify: `artifacts/studio/src/components/workspace/tabs/OpenWarehousesTab.tsx` (utilization header L56-58 + cell L66; warehouseId render L64)
- Modify: `artifacts/studio/src/components/workspace/tabs/AssignmentsTab.tsx` (ids L47-48)
- Test: both tab tests

**Interfaces — Consumes:** a new **optional** `displayedInputs` prop (capacityMode + added-entity displayCodes). Both tabs currently receive only `result` + `scenarioId`, and the `Workspace.tsx` call sites are frozen until T9. **Per-commit-green:** add the prop as **optional with a safe default** (no `displayedInputs` → utilization shown as today, ids rendered raw) so T6's commit typechecks standalone. **T9 passes the real `displayedInputs`** at the call sites (L2150/L2151 region), making the feature live. Note the exact prop name/shape here for T9.

**Steps:**
- [ ] Test: given `displayedInputs.capacityMode === "none"` → Utilization column hidden; uniform/per_wh/two-echelon → shown; **prop absent → shown** (back-compat).
- [ ] Test: a user-created warehouse (`aw-…`) from `displayedInputs.addedWarehouses` renders its **display ID** (not uid) in Open Warehouses + Customer Assignments; resolved from `displayedInputs`, not `localInputs`.
- [ ] Test (**two-echelon**): an added refinery from `displayedInputs.addedRefineries` (same `aw-` UID family) receives its stable display ID in Open Warehouses + Assignments — the `displayCodeById` map must be built from **both** `addedWarehouses` and `addedRefineries`.
- [ ] Test: an unsaved capacityMode/rename edit in `localInputs` does NOT change the displayed column/label (snapshot invariant — the prop is the snapshot).
- [ ] Implement optional `displayedInputs` prop + `displayCodeById` from `addedWarehouses` ∪ `addedRefineries` (mirror `DistancesTab`'s pattern).
- [ ] Run: `pnpm --filter studio test` (tabs) + `pnpm run typecheck`.
- [ ] Commit `[B2.2-T6] B1 util gate + B6 display-ID (optional displayedInputs prop; T9 wires call site)`.

**DoD:** with `displayedInputs` wired (by T9), utilization hidden at capacityMode=none and added warehouses show human ID from the snapshot in both tabs; commit typechecks without the call-site change.

---

### T8 — A3 frontend: customer Active/Excluded through dialogs **and** InputMapTab mutators

**Role:** frontend-engineer. Gated on **T1** (`addedCustomers[].status`) and **serialized after T3** (both edit `InputMapTab.tsx`).

**Two gates (review):**
1. **Role gate** — a new `EntityRoleConfig.supportsExclusion?: boolean` (**optional, default `false`**; explicitly `true` on `CUSTOMER_ROLE` only). Do NOT reuse `hasStatus` (both customer + station have `hasStatus:false`; that flag means facility status). Optional-with-default means warehouse/mine/refinery/station roles inherit `false` without per-role edits, and a future role can't accidentally acquire exclusion.
2. **Model gate for ADDED customers** — the added-customer status control is additionally gated on the model capability `supportsAddedCustomerExclusion` (us/2E true, **brazil false**), which `InputMapTab` **derives internally from `useListModels()` + its own `modelId`** (same pattern as OutputMapTab/CostSummaryTab) and passes to the dialogs — **no `Workspace.tsx` wiring needed**. **Base**-customer status stays role-gated only (allowed for Brazil too — spec permits base status for Brazil). So: base customer → role gate; added customer → role gate AND model capability.

**Files:**
- Modify: `artifacts/studio/src/components/workspace/map/types.ts` (`AddedCustomerInput` L20-28 → add `status?: "active" | "excluded"`; `EntityRoleConfig` → add `supportsExclusion?: boolean` optional default false; set `CUSTOMER_ROLE.supportsExclusion = true`)
- Modify: `artifacts/studio/src/components/workspace/map/dialogs/EditCustomerDialog.tsx` (Active/Excluded gated on `role.supportsExclusion`; for an **added** entity additionally require the `supportsAddedCustomerExclusion` prop; emit `status`)
- Modify: `artifacts/studio/src/components/workspace/map/dialogs/CreateEntityDialog.tsx` (`kind==="cs"` branch → same two-gate logic; a newly created customer is an added entity → requires the capability prop; emit `status`)
- Modify: `artifacts/studio/src/components/workspace/tabs/InputMapTab.tsx` — wire status through the parent (dialogs otherwise emit a discarded value): both p-median and two-echelon paths — (a) customer edit-patch type includes `status`; (b) base-customer override upsert writes `demand + status`, still pruning a true no-op override; (c) added-customer edit mutator retains/writes `status`; (d) create + copy defaults carry `status` (default `active`); (e) derive `supportsAddedCustomerExclusion` from `useListModels()` + `modelId` and pass it to both dialogs.
- Test: both dialog tests + `InputMapTab` integration tests + a role-config test

**Interfaces — Consumes:** T1's `status` field, T0's `supportsAddedCustomerExclusion` capability (via prop from T9), T3's `InputMapTab` toolbar structure. **Produces:** input payloads carry the selected customer `status`.

**Steps:**
- [ ] Test (role-config): every `EntityRoleConfig` constant resolves `supportsExclusion` (default false); only `CUSTOMER_ROLE` is true — so a new role can't accidentally get exclusion.
- [ ] Test: `EditCustomerDialog` (customer role) shows Active/Excluded, emits `{demand, status}`; **station role does NOT** (negative).
- [ ] Test: `CreateEntityDialog` customer branch shows the control; **station branch does NOT** (negative) — gated on `role.supportsExclusion`, not `kind`.
- [ ] Test (**Brazil-negative UI**): for an **added** customer with `supportsAddedCustomerExclusion:false` (Brazil), the status control is **hidden** in both dialogs; with `true` (us/2E) it shows. Base-customer status control still shows for Brazil.
- [ ] Test (integration): editing a **base** customer to Excluded → `inputs.customerOverrides` entry `status:"excluded"` (+ demand preserved); back to Active + default demand removes the no-op override.
- [ ] Test (integration): editing/creating an **added** customer to Excluded (capability on) → `inputs.addedCustomers` entry `status:"excluded"`; default `active`.
- [ ] Implement `types.ts`, both dialogs (two-gate), `InputMapTab` mutators + capability pass-down.
- [ ] Run: `pnpm --filter studio test` (dialogs + InputMapTab) + `pnpm run typecheck`.
- [ ] Commit `[B2.2-T8] A3 frontend: customer Active/Excluded (dialogs + InputMapTab, supportsExclusion role gate + supportsAddedCustomerExclusion model gate)`.

**DoD:** customer status lands in `inputs` (base → `customerOverrides.status`, added → `addedCustomers.status`) for us + two-echelon; Brazil shows base status but never added-customer exclusion; stations never show the control; roles default to no-exclusion; no-op overrides pruned.

---

### T10a — AppFooter component

**Role:** frontend-engineer. Leaf (no shell edits here).

**Files:**
- Create: `artifacts/studio/src/components/AppFooter.tsx` (bottom-center `© Developed by hx1`; export `FOOTER_H = 24`)
- Test: `AppFooter.test.tsx`

**Steps:**
- [ ] Test: renders `©` + `Developed by hx1`, centered, height `FOOTER_H`.
- [ ] Implement.
- [ ] Commit `[B2.2-T10a] AppFooter component`.

---

## Wave 1.5 (gated on T2)

### T7 — B3 frontend: DistancesTab read-only reference table + client-side view filter

**Role:** frontend-engineer. Gated on **T2** (hook + capability). `DistancesTab.tsx` itself is disjoint from `Workspace.tsx`, but its **call site provides no `modelId`/`capability`/`localInputs` today** — add these as **optional** props (safe default: hide the reference section when absent) so T7's commit typechecks; **T9 passes the real props at the `DistancesTab` render site**. Feature live once T9 lands.

**Virtualization:** use **`@tanstack/react-virtual`** (chosen per review option A — maintained, small; manual windowing rejected as needless). Add the dep in this task.

**Files:**
- Modify: `artifacts/studio/package.json` (+ `@tanstack/react-virtual`), `pnpm-lock.yaml` (regenerated — verify `pnpm install --frozen-lockfile` clean, per this repo's lockfile/Docker history)
- Modify: `artifacts/studio/src/components/workspace/tabs/DistancesTab.tsx` (reference section above the editable override grid; optional `modelId`/`referenceCapable`/`localInputs`-derived props)
- Test: `DistancesTab.test.tsx`

**Interfaces — Consumes:** `capabilities.supportsReferenceDistances` + `modelId` (fed via optional props from T9), live `localInputs`. **Hook usage (review):** call `useGetReferenceDistances(modelId, options)` (generated **path-parameter** signature — confirm exact shape after T2 regen; NOT `{id: modelId}`) **unconditionally** (Rules of Hooks) but with `enabled: supportsReferenceDistances && !!modelId` so an unsupported-but-valid model (e.g. Brazil) never fires the request (and never gets a 422). Set `staleTime: Infinity` (per-session) so remounting the tab does not revalidate — the matrix is fetched once per model.

**Steps:**
- [ ] Test: reference section renders only when the capability prop is true (capability, not modelId); absent/false → hidden (back-compat default).
- [ ] Test: an unsupported model (capability false, e.g. Brazil) makes **no** network request (query disabled), and remounting a supported tab does not refetch (`staleTime: Infinity`).
- [ ] Test: **client-side filter** against `localInputs` — the virtualizer's total **logical** row count (or a visible total indicator) is 5200 default → one base WH inactive → 5000 → one base customer excluded → 5174 → both → 4975 (instant, no refetch).
- [ ] Test: added entities absent from the reference section; a base×base `distanceOverrides` entry does NOT change the reference baseline value.
- [ ] Test: virtualization — only the **windowed subset** of rows is mounted in the DOM (not all 5200), asserted separately from the logical-count assertion above.
- [ ] Implement bulk fetch + `@tanstack/react-virtual` windowing + `localInputs`-derived filter.
- [ ] Run: `pnpm install --frozen-lockfile` (verify clean) + `pnpm --filter studio test Distances` + `pnpm run typecheck`.
- [ ] Commit `[B2.2-T7] B3 frontend: reference-distances table + client filter + react-virtual (optional props; T9 wires call site)`.

**DoD:** immutable matrix fetched once, filtered live against unsaved status edits; truly virtualized (windowed DOM); capability-gated; lockfile clean; commit typechecks without the call-site change.

---

## Wave 2

### T9 — `Workspace.tsx` omnibus (SINGLE WRITER)

**Role:** frontend-engineer. Runs after Wave 1/1.5 leaves. Consolidates every `Workspace.tsx` edit. Gated on: T1 (status), T6 (tab optional-prop shape), T7 (DistancesTab optional-prop shape), T10a (AppFooter).

**Files:**
- Modify: `artifacts/studio/src/pages/Workspace.tsx` only.

**Sub-changes (all in one task/commit sequence):**
1. **C2 header (items 13/14/15):** restructure header (L2189-2256) into `grid-cols-[auto_1fr_auto]`. Left: back-arrow + Scenario `<select>` (`select-scenario-context` moved from L2206-2223, testid preserved). Center (`1fr min-w-0`): chapter+summary (`workspace-chapter-summary`) — drop `max-w-[38%] truncate` + absolute positioning, full text, centered in track. Right: email + Logout (moved from L2244-2255). Remove title/subtitle block L2202-2205 (`text-app-name` goes away). Result-history stepper + "Save as scenario" + Run-Optimizer cluster stay in their current relative position (do not reposition).
   **Narrow-width behavior (review):** `grid-cols-[auto_1fr_auto]` alone does not guarantee a usable 375px layout (side tracks can eat the center). Below a breakpoint (e.g. `< md`/`< 768px`), **stack/wrap** the three zones into rows (selector row / summary row / account row) rather than a single overflowing row, keeping the stepper + Save placement unchanged and the header height contributing (no fixed height). Summary truncates gracefully at narrow widths instead of forcing horizontal overflow.
2. **C1 footer viewport:** mount `<AppFooter/>` inside the `h-screen flex flex-col` column (root L2188, inside `.scn-theme`); reserve `FOOTER_H` so the body (`flex-1` L2310) doesn't overlap map content.
3. **B4 tab order:** reorder `OUTPUT_ENTRIES` (L732-739) → Output Map, **Solution Summary (`cost-summary`)**, Open Warehouses, Customer Assignments, [Flows], Service Stats.
4. **B2 sidebar gate:** gate the output entries passed to `<SidebarTree outputs=...>` (L2317) by `activeModelManifest?.capabilities.outputGrids` (reuse the `entityToCapability` map at L2135-2141) → Flows disappears from the p-median-us sidebar. Content gate L2142-2148 stays as defense-in-depth.
5. **A3 effective-map projection:** `pmedianMapCustomers` L356 — derive `excluded` from the added customer's `status` instead of hardcoded `false`; apply the same to the two-echelon added-customer projection (locate its analogue; transport stations keep `false` — no excluded concept).
6. **B6/B1 tab call-site props:** pass the `displayedInputs` snapshot to `OpenWarehousesTab` + `AssignmentsTab` at their render sites (L2150/L2151 region) — the tabs added the optional prop in T6; this is where it becomes live.
7. **B3 DistancesTab call-site props:** pass `modelId`, the `supportsReferenceDistances` capability, and the `localInputs` the reference filter needs to `DistancesTab` at its render site — T7 added the optional props; this wires them.

**Steps:**
- [ ] Tests (add to `Workspace.test.tsx` / model-specific): header grid has selector-left, centered untruncated summary, email/logout top-right, stepper+Save unmoved; **narrow-viewport (375px) header** stacks/wraps with no horizontal overflow, summary truncates, all controls reachable; footer present inside viewport; tab order (Solution Summary 2nd); Flows absent for p-median-us sidebar, present for a flows model; added excluded customer projects `excluded:true`; `OpenWarehousesTab`/`AssignmentsTab`/`DistancesTab` receive their new props (util column hidden at capacityMode=none end-to-end; reference section appears for p-median-us).
- [ ] Implement all seven sub-changes.
- [ ] Run: `pnpm run typecheck && pnpm --filter studio test`.
- [ ] Commit `[B2.2-T9] Workspace omnibus: C2 header, C1 footer, B4 order, B2 sidebar gate, A3 projection, T6/T7 call-site wiring`.

**DoD:** header/footer/tab-order/sidebar-gate/projection all correct; T6 + T7 features live via wired call sites; preserved testids (`select-scenario-context`, `workspace-chapter-summary`); no `Workspace.tsx` edits elsewhere.

---

### T10b — Footer mounts in remaining shells

**Role:** frontend-engineer. Disjoint from `Workspace.tsx` (may run alongside T9). Gated on T10a.

**Files:**
- Modify: `artifacts/studio/src/components/AppShell.tsx` (root `h-screen flex flex-col` L32-33 → mount `<AppFooter/>`, reserve `FOOTER_H`)
- Modify: `artifacts/studio/src/pages/Login.tsx` (L40) + `artifacts/studio/src/pages/Register.tsx` (L44) → mount `<AppFooter/>`
- Test: `AppShell` / auth-page render tests assert footer present + narrow-screen no-overlap

**Steps:**
- [ ] Test: footer renders on AppShell-wrapped pages (Landing/Studio), Login, Register; narrow viewport (e.g. 375px) → no map/footer overlap on a Workspace-height assertion (covered in T9) and no clipping on AppShell.
- [ ] Implement mounts, reserving `FOOTER_H` in each `h-screen` column.
- [ ] Commit `[B2.2-T10b] mount AppFooter in AppShell/Login/Register`.

**DoD:** `© Developed by hx1` on every current route (Landing, Login, Register, Workspace, Studio); Compare excluded (route removed).

---

## Wave 3

### T11 — QA gate + accuracy + live verify

**Role:** qa-sdet.

**Steps:**
- [ ] Full repository gate:
  ```bash
  pnpm run typecheck && pnpm --filter api-server test && pnpm --filter studio test \
    && (cd artifacts/api-server/src/solver && python3 -m pytest tests/ -x)
  ```
- [ ] A3 touches the merge/solver **payload boundary** → run `python3 e2e_accuracy.py` and `python3 e2e_journey.py` (from `artifacts/api-server/src/solver/tests/`) **unmodified**; confirm no accuracy regression (`solve.py` itself unchanged).
- [ ] Live verify (claude-in-chrome, local dev, disposable account, purge after): footer on all pages; header layout (selector left / summary centered untruncated / email+logout right / stepper+Save unmoved); input-map checkboxes + size-by-demand + legend-follow; customer Active/Excluded in both dialogs (station has none); route hover tooltip; utilization hidden at capacityMode=none; Flows gone from p-median-us sidebar; Distances reference table + live filter; Solution Summary 2nd + compare open-facility-by-city row; added-warehouse display ID in Open Warehouses/Assignments.
- [ ] Ledger + CLAUDE.md update; controller merges to local `main`.

**DoD:** full gate green; `e2e_accuracy.py`/`e2e_journey.py` unmodified & passing; all 15 items verified live.

---

## Self-Review (author checklist)

- **Spec coverage:** items 1(C1/T10a+T10b+T9-footer), 2/3(A1/T3), 4(A2/T3), 5(B1/T6), 6(B2/T9), 7(B3/T0+T2+T7+T9-wiring), 8(B4/T9), 9(B5/T5), 10(A3/T0+T1+T8+T9-projection), 11(B6/T6+T9-wiring), 12(A4/T4), 13/14/15(C2/T9). All mapped.
- **Types:** `addedCustomers[].status` defined T0(manifest)+T1(Zod), consumed T8/T9; `EntityRoleConfig.supportsExclusion?` optional-default-false T8 (distinct from `hasStatus`); `supportsAddedCustomerExclusion` T0(schema+manifest us/2E true, brazil false)→T1(payload/precheck gate)→T2(registry/openapi)→T8(dialog gate via `useListModels`); `FIXED_CUSTOMER_RADIUS` T3; `supportsReferenceDistances` T0→T2(registry/openapi)→T7(prop, `enabled` guard, `staleTime:Infinity`)→T9(wire); `useGetReferenceDistances(modelId, options)` T2→T7; `FOOTER_H` T10a→T9/T10b; `openFacilityIds()`+`useGetDataset` reused T5.
- **Serialization:** manifests/`ManifestSchema` single writer (T0); `Workspace.tsx` single writer (T9); `openapi.yaml` single writer (T2); `InputMapTab.tsx` T3→T8 serialized. T6/T7 optional props keep every commit green; T9 wires call sites. No concurrent shared-file writers.

---

## Review Resolution — Rev 1 (2026-09-02)

Review below (user-authored) triaged; every P1/P2 resolved into the tasks above:
- **P1 T8 InputMapTab wiring** → T8 now edits `InputMapTab.tsx` (base + added mutators, both models) + integration tests proving `status` lands in `inputs`; serialized after T3.
- **P1 `role.hasStatus` wrong flag** → new `EntityRoleConfig.supportsExclusion` (customers true, stations false); used in both dialogs; negative station coverage for Edit **and** Create.
- **P1 T1 manifest inputsSchema** → new **T0** (solver-engineer) owns both manifests' `addedCustomers.status` + p-median-us `supportsReferenceDistances` + `ManifestSchema` + tests; T1/T2 no longer touch manifests → genuinely file-disjoint. Solver-owned seam coordinated per AGENTS.md.
- **P1 T6/T7 atomic call-site wiring** → T6/T7 add optional props (per-commit-green); T9 (single `Workspace.tsx` writer) supplies real props (sub-changes 6 + 7). T8 removed from parallel Wave 1 (Wave 1.5, after T1 + T3).
- **P2 T5 existing Open facilities block** → T5 replaces/moves the count row (no duplicate), gates the new city-list row on `supportsFacilityStatus`, keeps Aggregate utilization under `supportsP`, tests two-echelon = exactly one city-list row + no aggregate-utilization row, covers both `addedWarehouses`/`addedRefineries` keys.
- **P2 B3 ETag with global etags off** → T2 sets an **explicit** ETag from the dataset version/hash + `Cache-Control: public, max-age=0, must-revalidate`; tests `If-None-Match` → **304**.
- **P2 T7 virtualization** → **`@tanstack/react-virtual`** (option A); dep + `pnpm-lock.yaml` in T7 file list, `--frozen-lockfile` check; tests assert logical total vs windowed-DOM subset separately.

### Review Comments — 2026-09-02 (verbatim)

**Review status:** Needs revision before execution. The plan tracks the approved Rev 2 product decisions, including the model-scoped immutable reference matrix, but several task boundaries do not yet implement their stated interfaces end to end.

**P1 — T8 does not wire customer status through `InputMapTab`.** T8 changes only `map/types.ts`, `EditCustomerDialog.tsx`, `CreateEntityDialog.tsx`. Current `InputMapTab.tsx` edit paths accept demand-only customer patches; p-median and two-echelon mutators update only `demand`. A dialog can emit `status`, but the parent discards it for base and added customers. Add `InputMapTab.tsx` to A3 frontend and update, both models: customer edit-patch type includes `status`; base-customer override upsert writes demand + status while removing a true no-op; added-customer edit mutator retains/writes status; create path + copy defaults; integration tests proving `onInputsChange` contains the selected status.

**P1 — `role.hasStatus` cannot distinguish customers from stations.** Both `CUSTOMER_ROLE.hasStatus` and `STATION_ROLE.hasStatus` are `false`; that flag describes warehouse/refinery facility status. Introduce a separate semantic flag (`supportsExclusion`/`hasCustomerStatus`), set for customers not stations, used in both Create and Edit dialogs. Add negative coverage for the station version of each dialog.

**P1 — T1 omits the manifest `inputsSchema` changes required by A3.** Both `solvers/p-median-us/manifest.json` and `solvers/two-echelon-gold-au/manifest.json` describe `addedCustomers` without `status`. If T1 changes only Zod, public model metadata still advertises the old shape. Add `status` with the enum + back-compat default/optionality to both manifests + manifest-schema tests. This invalidates Wave 1's file-disjoint claim: T1 and T2 both touch the p-median-us manifest — serialize or assign that manifest to one writer. Coordinate the solver-owned manifest seam per AGENTS.md.

**P1 — T6/T7 require `Workspace.tsx` wiring that no task supplies atomically.** T7 consumes `modelId`/`supportsReferenceDistances`/`localInputs`, but `DistancesTab` props + call site provide none, and T9's sub-changes contain no B3 wiring. T6 same: `OpenWarehousesTab`/`AssignmentsTab` receive only `result` + `scenarioId`, so introducing required snapshot data without call-site updates leaves an intermediate commit that does not typecheck. Revise the graph so component + call-site land atomically while preserving a single serialized `Workspace.tsx` writer (move T6/T7 into the Workspace wave, or allow that writer to make the T6/T7 call-site changes and run sequentially). Remove T8 from parallel Wave 1 until T1 lands; serialize its `InputMapTab.tsx` work with T3.

**P2 — T5 must replace/split the existing Open facilities block.** `CostSummaryTab` already renders an Open facilities count + Aggregate utilization in one `supportsP` fragment. Specify: replace/move the existing count row (no duplicate); gate the new city-list row independently with `supportsFacilityStatus`; keep Aggregate utilization under `supportsP`; test two-echelon gets exactly one city-list row but no Aggregate utilization row. Cover both `addedWarehouses` (p-median) and `addedRefineries` (two-echelon) in tests.

**P2 — B3 cache validation is incomplete with global ETags disabled.** The API sets `app.set("etag", false)`, so T2 cannot rely on Express to generate the ETag. Require the route/loader to derive + set a stable ETag explicitly (from dataset version/hash) and test `If-None-Match` → `304`. Make freshness explicit: `public, immutable` without a lifetime helps little, and a long `max-age` on a stable unversioned URL can serve stale data after a dataset expansion. Use a versioned URL/key with immutable caching, or ETag revalidation such as `public, max-age=0, must-revalidate`.

**P2 — T7 has no selected virtualization mechanism.** No virtualization package installed; T7 lists only `DistancesTab.tsx` + test but its DoD requires true row virtualization. Choose before dispatch: add `@tanstack/react-virtual` (incl. `package.json` + `pnpm-lock.yaml`), or specify the manual windowing algorithm + assumptions + tests. Filter-count tests should assert the virtualizer's total logical row count (or a visible total indicator) while separately asserting only the windowed subset is mounted.

---

## Review Resolution — Rev 2 (2026-09-02)

Rev 2 review (below, verbatim) triaged; all P1/P2 resolved:
- **P1 A3 leaks into Brazil** → new model capability `supportsAddedCustomerExclusion` (T0: us/2E true, brazil false + `ManifestSchema`). T1 gates payload + precheck on it (Brazil-negative tests). T8 gates the added-customer dialog control on it (derived via `useListModels`; Brazil-negative UI test). Base-customer status still allowed for Brazil. Exposed publicly by T2.
- **P1 T5 no city-name source** → T5 adds `useGetDataset({ modelId })` inside the tab (base id→city) ∪ each column's `addedWarehouses`/`addedRefineries`; unknown-id + pre-load fallback = raw id/code; base + added name tests.
- **P1 T7 must gate the query** → call `useGetReferenceDistances(modelId, options)` (generated path-param signature) unconditionally with `enabled: supportsReferenceDistances && !!modelId`; `staleTime: Infinity`; test unsupported model makes no request.
- **P1 T3 fixed mine under Refineries layer** → mine marker gated by the refinery layer toggle; test that disabling Refineries hides refineries + mine.
- **P2 `supportsExclusion` type contract** → made **optional, default false** on `EntityRoleConfig` (only `CUSTOMER_ROLE` true) + a role-config test so no new role auto-acquires exclusion.
- **P2 B6 two-echelon display-ID** → `displayCodeById` built from `addedWarehouses` ∪ `addedRefineries`; two-echelon added-refinery test in Open Warehouses + Assignments.
- **P2 header narrow width** → `< md` breakpoint stacks/wraps the three zones (stepper+Save unchanged); 375px header test (truncation, control access, no horizontal overflow).
- **P2 OpenAPI conditional responses** → T2 documents `200`/`304`/`422` + generated-client tests; "known-but-unsupported" (ownerless), not "owned".

### Review Comments — Rev 2 (2026-09-02, verbatim)

**Review status:** Improved substantially, but another revision is needed before execution. The Rev 1 findings are addressed; the findings below were identified by tracing the revised task contracts against the current implementation.

**P1 — A3 still leaks added-customer exclusion into Brazil.** T8 makes `CUSTOMER_ROLE.supportsExclusion = true`, but p-median Input Map mode is shared by US and Brazil; `pMedianInputsSchema`, `buildPayload`, `buildActivePMedianIds` are shared too. As written, the Brazil UI and precheck can treat an added customer as excluded while the Brazil solver still serves it, contrary to A3's omission of Brazil. Add a model capability such as `supportsAddedCustomerExclusion`, gate added-customer Create/Edit controls + payload/precheck semantics with it, add Brazil-negative UI/payload/precheck tests. Keep distinct from the semantic customer role: base-customer status rendering is still permitted for Brazil; added-customer exclusion is model-scoped.

**P1 — T5 has no source for base-facility city names.** T5 gives each compare column only persisted `inputs` + `result`, but results contain facility IDs not city names; `CostSummaryTab` has no dataset prop, only `useListModels`. Specify one complete data path: pass the loaded dataset from T9's `Workspace.tsx`, or fetch inside the tab with `useGetDataset({ modelId })`. Define loading behavior + fallback for an unknown facility ID, and test both base and added facility names.

**P1 — T7 must gate the query, not only hide the section.** Hiding the section when `supportsReferenceDistances` is false does not stop a valid-but-unsupported model (Brazil) from invoking the endpoint and getting `422`. Call the hook unconditionally (Rules of Hooks) but pass `enabled: supportsReferenceDistances && !!modelId`; test unsupported models make no request. Use the generated path-parameter signature `useGetReferenceDistances(modelId, options)`, not `{ id: modelId }`. Set `staleTime: Infinity` so remounting the tab does not revalidate a once-per-model query.

**P1 — T3 does not put the fixed mine under the Refineries layer.** Spec defines two-echelon layers as "Refineries, Customers," mine implicit under Refineries. The fixed mine marker is rendered outside `EntityMarkers`, so it stays visible when Refineries is off. Gate the mine with the refinery/warehouse layer toggle; test that disabling Refineries hides both refinery markers and the mine.

**P2 — `supportsExclusion` needs a complete type contract.** If required on `EntityRoleConfig`, every role must define it; T8 specifies only customer true/station false. Make it optional default false, or set false on warehouse/mine/refinery/every non-customer role. Add a type/role-config test so a new role cannot acquire exclusion accidentally.

**P2 — B6 needs two-echelon display-ID coverage.** T6 tests only `addedWarehouses`; the same components serve two-echelon where added facilities live under `displayedInputs.addedRefineries` (`aw-` UID family). Build the display-code map from both collections; add a two-echelon test proving an added refinery gets its stable display ID in Open Warehouses + Assignments.

**P2 — The header layout is underspecified at narrow widths.** `grid-cols-[auto_1fr_auto]` does not guarantee a usable 375px layout: selector + account/result controls can consume width before the center, even with `min-w-0`. Specify a breakpoint that stacks/wraps the three zones while leaving stepper + Save placement unchanged; add a narrow-viewport header test (truncation, control access, no horizontal overflow).

**P2 — OpenAPI must document the conditional reference-distance responses.** T2 requires `304` and `422` but its OpenAPI step only calls out the `200 ReferenceDistances` response. Add explicit `304` no-content + `422` error responses to the operation + generated-client tests. Replace "owned-but-unsupported model" with "known-but-unsupported," because the endpoint is ownerless.
