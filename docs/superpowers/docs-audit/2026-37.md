# docs-audit — week 2026-37

Sweep of 55 in-scope docs. Mechanical layer found 125 candidates (stale_reference 92, superseded 4,
conflicting_instruction 29). This first PR processes the two highest-confidence, unambiguous findings
(one commit each) and dismisses the false positives below; the remaining candidates carry over to the
next sweep for verification (the mechanical layer is deliberately noisy — findings are only what
survives judgment).

## Sweep 2026-09-12

Summary: 2 findings (2 commits), 4 dismissed, ~119 candidates carried over for later verification.

### Finding a2652f6a6d · stale_reference · repo · high · replace · commit ad8c360
- **Where:** `README.md` (intro L3, repo-layout L22-24, product L65)
- **Evidence:** describes the Arcadia gamification layer (quests/XP/badges/leaderboard,
  `src/pages/arcadia/`, `ArcadiaShell`, `GamificationContext`) as existing.
- **Rests on:** all removed in Phase 1 (commit `44cb9a6` deleted `pages/arcadia/` +
  `GamificationContext`; Studio → Workspace). No matching source today.
- **Proposal (applied on branch):** removed the Arcadia sentences from the intro, repo-layout, and
  product sections.

### Finding 86a9321a55 · stale_reference · repo · high · remove · commit 5528853
- **Where:** `.agents/memory/studio-stack.md` "API compare endpoint" section (L12-16)
- **Evidence:** documents `POST /api/scenarios/compare` — route not in `openapi.yaml`.
- **Rests on:** commit `c045548` (Phase 3.2 Task 1) removed the compare route + Compare page.
- **Proposal (applied on branch):** removed the stale compare-endpoint section.
  - Note: this file also claims "the solver is pure TypeScript … Python/PuLP unavailable", which is
    false (the app uses Python/PuLP/CBC). Flagged as a carried-over candidate for a follow-up finding
    (larger rewrite than this focused PR).

## Sweep 2026-09-12 (second run)

Summary: 1 new finding (stacked onto this open PR — the original two commits are unchanged, no
duplicates).

### Finding 3e07dc8051 · stale_reference · repo · high · delete-file · commit 357039f
- **Where:** `docs/ops/stack-proof.md:4`
- **Evidence:** references a nonexistent path `artifacts/ghost/stack-proof-nonexistent.ts`.
- **Rests on:** path not on disk; the file has no real referent.
- **Proposal (applied on branch):** delete the file.

## Carried over

- Findings `a2652f6a6d`, `86a9321a55` from the first sweep — unchanged, still open (age 0 weeks).

## Dismissed

- `83afba2394` superseded `.claude/skills/docs-apply/SKILL.md` — false positive: shares generic
  headings ("Steps"/"Notes") with `harness-retro/SKILL.md` but they are distinct skills, neither
  supersedes the other.
- `87d78ff9a0` superseded `docs/superpowers/prompts/jobs/docs-audit-sunday.md` — false positive: same,
  vs `harness-weekly.md`; two different cron jobs.
- `ab7341c93d` superseded `CLAUDE.md` — false positive: "superseded by" appears in prose describing
  history (R0.8), not a file-supersede marker.
- `dd08b0fca9` superseded `IMPLEMENTATION_PLAN.md` — false positive: "superseded by Phase 3.5" is
  prose about `pmedian.ts`, not a document supersede.

## Sweep 2026-09-12 (harness-weekly combined run, branch `harness-weekly/2026-37-1`)

Full re-run of the judgment pass against a freshly regenerated `.harness/docs-audit/candidates.json`
(111 candidates — down from the first sweep's 125 because the two `## Sweep 2026-09-12` fixes above
had already landed on `main` by the time this ran). **Result: zero new findings.** Every candidate
resolved to either a suppressed duplicate of an id already dismissed above (within the 8-week
window) or a false positive traced to one of five mechanical-detector bugs. No repo file is touched
by this run besides the report/inventory refresh and this findings file.

### Mechanical false-positive classes identified

1. **Keyword collision, no context (60 candidates).** `type=stale_reference`,
   evidence `"no matching source (Arcadia/gamification removed in Phase 1/A3)"`. The detector flags
   any line containing "arcadia"/"badge"/"gamif*"/"quest"/"XP" regardless of tense. Sampled one line
   per distinct file (12 files) — every hit was either the unrelated shadcn `Badge` UI component
   (`docs/design-system/**`), the literal string `"arcadia-dev-secret"`
   (`application-audit-and-remediation-plan.md`), or past-tense prose correctly describing Arcadia's
   *removal* (CLAUDE.md's own Phase 1/A3 changelog entries, `IMPLEMENTATION_PLAN.md`'s pre-Phase-1
   repo-map baseline, the PRD's stated rationale for removing it, the Chapter-10 docs' explicitly
   *rejected* "M5 — Arcadia quest" task, and `application-audit-and-remediation-plan.md`'s own audit
   finding — already fixed via `a2652f6a6d` above — that Arcadia does *not* exist). Ids:
   `eecf33834c`, `926c6a77bc`, `5045dd84f5`, `9e29497a46`, `823c116496`, `98500a77e1`, `e899ddf536`,
   `2910276b25`, `b10b9a7266`, `07c33e80e9`, `a67c2145d5`, `74c4dd3471`, `7155380d0f`, `2d5d4286c3`,
   `544e12ec02`, `ae2f4f7fb2`, `20f2c3faf5`, `c9518fe5b0`, `ca2396b505`, `b9146906a8`, `a59df5b383`,
   `21f8478991`, `b31903fdc4`, `bf84169565`, `5c4ea68057`, `42e1b5fc97`, `c6308f971f`, `48d7062b7f`,
   `0fa59ed728`, `b1881aa109`, `c048ef36a2`, `ffd6efc6b4`, `af198d52ff`, `1e0c81df3a`, `ac832f2bbf`,
   `1263138bf8`, `9aa527caa0`, `1749ec64d4`, `2742d9e590`, `6fdd45907d`, `8c083835b6`, `dcc6539702`,
   `fb4ea87688`, `c8efe4c0e9`, `8b3986f564`, `c7cb964d80`, `9aad117959`, `6b2b9fb6b8`, `78a557bb4f`,
   `274f60519f`, `c9ab3fef06`, `9b6596865c`, `caf1b8cc0d`, `82ba35f8df`, `d42e0b7c00`, `5edd629ba9`,
   `89f28aea28`, `18b1f310c9`, `8902479de7`, `4517204d42`.
2. **"Conflicting instruction" is text-similarity, not a semantic check (24 candidates).** All of the
   form "opposing instruction vs CLAUDE.md:N". Sampled 8 pairs across 6 distinct evidence groups
   (including both pairs repeated 5x/3x against CLAUDE.md:123/:274) — every pair either agrees
   verbatim (e.g. `README.md:65` vs `CLAUDE.md:96`, both state the identical OpenAPI-first codegen
   rule) or shares vocabulary with no logical relationship. Zero genuine contradictions found. Ids:
   `c37e7665b9`, `3f255a0749`, `57e0ec36d2`, `57a9a1a147`, `d3c5270f5b`, `d9b8fc90ad`, `f37abb5680`,
   `77d432d322`, `17f5d1ea56`, `e770b47640`, `87bcca9b77`, `452b4ece16`, `8070c32a56`, `751f5f3630`,
   `8941548f9b`, `6bf62d90f8`, `ab9c051e48`, `480a4cdb28`, `4272e3a023`, `8d6412dd8d`, `ea84d9ffec`,
   `e11ca1a4cf`, `60c31f9321`, `f099113907`.
3. **Glob/placeholder syntax read as a literal path (9 candidates).**
   `solvers/p-median-us/dataset/*.json`, `.../{warehouses,customers}.json`,
   `solvers/<model-id>/manifest.json`, `.../dataset/version.json`, `docs/superpowers/gates/<cause>.md`,
   `docs/product-insights/<date>.md`, `docs/superpowers/docs-audit/*.md` / `YYYY-WW.md` all resolve to
   real matching files once expanded by hand, or are explicitly documented as not-yet-generated
   artifacts. Ids: `ecfe1d671a`, `79464fe679`, `0ce6106865`, `6458079e54`, `20a27fc8a1`, `20791f6e95`,
   `85a502cf36`, `029ecc3635`, `7faa592cc5`.
4. **Escaped-backtick markdown parsed into the path (2 candidates).**
   `docs/design-system/ui_kits/studio/README.md` writes `` \`artifacts/studio/src\` `` (escaped
   backticks around an inline code span); the detector captured the trailing `\` as part of the path.
   The real paths (`artifacts/studio/src`, `lib/chapters.ts` → `artifacts/studio/src/lib/chapters.ts`)
   exist. Ids: `08406815c0`, `7632b72920`.
5. **Package-relative shorthand mistaken for repo-root-relative (7 candidates).**
   `model-integration-precheck.md`'s `lib/mapBounds.ts`/`lib/entityId.ts`/`lib/gazetteer.ts` and
   `docs/product-insights/POSTHOG-AUDIT.md`'s `lib/posthog.ts`/`lib/api-spec/openapi.yaml:573-584`
   both follow the same package-relative convention already used for every other path in the same
   document (POSTHOG-AUDIT.md's own `routes/auth.ts`, `app.ts`, `index.ts` are all relative to
   `artifacts/api-server/src/`, not repo-root `lib/`). Real files:
   `artifacts/studio/src/lib/{mapBounds,entityId,gazetteer}.ts`,
   `artifacts/api-server/src/lib/posthog.ts`; the openapi.yaml line range is accurate (`AuthUser`
   schema, lines 572-584). CLAUDE.md itself uses the identical shorthand throughout its own changelog
   (e.g. "New `lib/mapBounds.ts`" in the E5.1 entry). Ids: `ca27400a9a`, `a5b45c2802`, `b9c2c36b1a`,
   `f7aa5a8ba0`, `94c200dfd1`, `48e0e2fae7`, `922216e1ce`.

### Individually verified (not part of a bulk class)

- `cc8e5c47f5`, `7da41eab63`, `b930279666`, `13168c10ea`, `4437457f1c`, `a3979e4649` — "route not in
  openapi.yaml" against `IMPLEMENTATION_PLAN.md`/`PRD-network-optimization-studio-v2.md`/`CLAUDE.md`.
  The underlying fact is real (`POST /scenarios/compare` and `POST /scenarios/:id/reset-to-baseline`
  were removed from `openapi.yaml` in Phase 3.2 Task 1, commit `c045548`; the legacy `POST /login`
  was removed in A1.1). But `IMPLEMENTATION_PLAN.md` and the PRD are frozen pre-execution design
  docs, and CLAUDE.md's own hard rule #8 ("when the plan conflicts with the repo's actual state,
  trust the repo, ... note the deviation") explicitly licenses exactly this kind of plan/repo drift
  rather than obliging a retroactive doc fix — the CLAUDE.md hits are themselves accurate historical
  changelog entries, not live claims. Resolved via that stated policy, not treated as ambiguous.

### Suppressed (dismissed within the last 8 weeks, not re-verified)

- `ab7341c93d`, `dd08b0fca9`, `83afba2394` — same three `superseded` ids dismissed above in the
  `## Dismissed` section from this week's first sweep; carried forward under the 8-week suppression
  rule rather than re-litigated.

### Carried over

None — every one of the 111 candidates resolved to dismissed/suppressed this run.
