# docs-audit — week 2026-38

Sweep of 55 in-scope docs. Mechanical layer found 170 candidates (stale_reference 125, superseded
11, conflicting_instruction 35 — includes 3 ids recurring from a prior sweep, suppressed below). Of
those, 1 is a real finding (fixed, one commit); 3 are genuinely ambiguous (noted, not guessed); the
rest are false positives from three systematic gaps in the mechanical checker, verified against real
code/git history and dismissed below.

Summary: 1 finding (1 commit), 162 dismissed, 3 suppressed (recurring within the 8-week window), 3
noted as ambiguous.

## Findings

### Finding b62d082226 (+ c8b41e439b) · stale_reference · repo · high · replace · commit 93611ec
- **Where:** `.agents/memory/studio-stack.md`, "Solver decision" section (L8).
- **Evidence:** claims "the solver is a pure TypeScript greedy + 1-opt local search" and that
  Python/PuLP was unavailable ("uv not in PATH").
- **Rests on:** `artifacts/api-server/src/solver/jobRunner.ts` spawns real `python3 solve.py`
  (PuLP/CBC), verified present on disk and referenced throughout `CLAUDE.md`'s history (G2.1, the
  sacred `e2e_accuracy.py`, `requirements.txt` pinning `pulp==3.3.2`). The claim was false when
  written and remains false — this is not staleness from drift, it's a factual error that survived
  in a memory file agents actually read.
- **Proposal (applied):** replaced the section with an accurate description of the real Python
  solver pipeline and why it matters (don't skip Python/CBC test coverage).
- **Note:** this file also documents `result`-JSONB storage and the utilization 0-100 convention
  correctly — only the "Solver decision" section was wrong; left the rest untouched.
- This exact issue was flagged as a carried-over candidate in last week's sweep (2026-37, see its
  `## Finding 86a9321a55`) and deferred as "a larger rewrite than that focused PR" — fixed here.

## Noted as ambiguous (not fixed, not dismissed)

- **`7f2ddaa345`, `117589b546`, `c8e39bbc12`** — `AGENTS.md:13` describes a "shared task list
  (`TaskCreate`/`TaskList`/`TaskUpdate`)" for cross-agent visibility. These three tool names are not
  in this session's deferred-tools list (unlike `SendMessage` on the same line, which is real and
  dismissed below as a false positive). Whether they exist depends on session/harness configuration
  this sweep cannot observe from inside one session — genuinely can't verify from repo/git alone.
  Left for a human to confirm against the actual agent-team harness before editing `AGENTS.md`.

## Suppressed (dismissed within the last 8 weeks — 2026-37 sweep, not re-listed as new)

- `83afba2394` superseded `.claude/skills/docs-apply/SKILL.md` — recurs verbatim; same false
  positive as 2026-37 (shares generic headings with `harness-retro/SKILL.md`, doesn't supersede it).
- `ab7341c93d` superseded `CLAUDE.md` — recurs verbatim; same false positive (the "superseded by"
  text is prose about R0.8 history, not a document-supersede marker).
- `dd08b0fca9` superseded `IMPLEMENTATION_PLAN.md` — recurs verbatim; same false positive
  ("superseded by Phase 3.5" is prose about `pmedian.ts`, not a document supersede).

## Dismissed (162 ids, grouped by root cause — each spot-checked against real code/git, several
verified exhaustively across every file in the group)

**(A) Glob/brace/placeholder paths the checker doesn't resolve, but the real file/dir exists** — the
checker treats `X/**`, `X/{a,b,c}/**`, and `<model-id>`/`<cause>` placeholders as literal paths
instead of patterns; every concrete instance checked (`artifacts/api-server/src/routes/`,
`lib/db/`, `scripts/src/`, `.github/workflows/`, `artifacts/studio/src/`,
`lib/api-client-react/src/generated/`, `lib/api-zod/src/generated/`,
`artifacts/api-server/src/solver/tests/`, `artifacts/studio/e2e/`, `solvers/p-median-us/manifest.json`,
`solvers/p-median-us/dataset/version.json`, `solvers/p-median-us/dataset/{warehouses,customers}.json`,
`docs/superpowers/docs-audit/2026-37.md`, `docs/superpowers/gates/doc_drift.md`,
`artifacts/api-server/src/solver/{solve.py,merge_inputs.py,jobRunner.ts,pmedian.ts}`) exists on disk.
  - `00e7b00eac, 0137985b7b, 0ce6106865, 0fb2ce8b2e, 111f12b9ad, 1c588f46d3, 20791f6e95, 20a27fc8a1, 22b3131f8c, 442671f614, 6458079e54, 6f54ae2ced, 725333fec0, 73de31eec5, 7701b26724, 79464fe679, 7b491bc0b3, 8343a6178b, 8acd2d669f, a5b45c2802, b9c2c36b1a, bf82ebec6f, c3e21e811e, c64a17321f, ca27400a9a, cb8dd79643, d2064bdaf8, d417378022, d455cafdda, e2540748c6, e9afffc14d, ecfe1d671a`

**(B) `SendMessage` flagged "symbol not found in source"** — it's a real, currently-available
deferred harness tool (confirmed present in this session's own tool list), not a repo-defined
symbol, so a repo-wide grep will never find it. Used correctly everywhere it's referenced.
  - `07dbb35596, 0b00dcf741, 1fced54826, 4198cdb0d9, 6a226fc0b0, e57dad81a1`

**(C) `devops-engineer.md` "pnpm script not found: update / audit"** — `pnpm update` and
`pnpm audit` are built-in pnpm subcommands, not `package.json` scripts; no script entry is expected.
The "audit" mention is also literally describing an absence ("no `pnpm audit` ... gate exists in CI
today"), not a claim that one exists.
  - `e1eaf51ad7, f3b98bc13e`

**(D) "no matching source (Arcadia/gamification removed in Phase 1/A3)"** — this generic evidence
tag is keyword-matched, not context-aware, and is wrong in three distinct ways verified across every
file it appears in: (1) most hits are correct **past-tense history** ("Deleted `pages/arcadia/`",
"removed in Phase 1/A3") describing work that genuinely happened — accurate, not stale; (2) several
hits in `docs/design-system/**` match the unrelated UI component word "Badge" (`<Badge
variant="success">`), not the gamification "badge" concept at all; (3) several hits (e.g. `CLAUDE.md`
lines 171/184/191/272/275/293) don't mention Arcadia/gamification anywhere near the cited line —
misattributed line numbers. Spot-verified against real file content in every distinct source file in
this group (`CLAUDE.md`, `IMPLEMENTATION_PLAN.md`, `PRD-network-optimization-studio-v2.md`,
`application-audit-and-remediation-plan.md`, both chapter-10 docs, 5 `docs/design-system/**` files,
`docs/ops/e2e-stale-specs.md`) before dismissing the whole class.
  - `07c33e80e9, 0fa59ed728, 1263138bf8, 1749ec64d4, 18b1f310c9, 1e0c81df3a, 20f2c3faf5, 21f8478991, 2742d9e590, 274f60519f, 2910276b25, 2d5d4286c3, 42e1b5fc97, 4517204d42, 48d7062b7f, 5045dd84f5, 544e12ec02, 5c4ea68057, 5edd629ba9, 6b2b9fb6b8, 6fdd45907d, 7155380d0f, 74c4dd3471, 78a557bb4f, 823c116496, 82ba35f8df, 8902479de7, 89f28aea28, 8b3986f564, 8c083835b6, 926c6a77bc, 98500a77e1, 9aa527caa0, 9aad117959, 9b6596865c, 9e29497a46, a59df5b383, a67c2145d5, ac832f2bbf, ae2f4f7fb2, af198d52ff, b10b9a7266, b1881aa109, b31903fdc4, b9146906a8, bf84169565, c048ef36a2, c6308f971f, c7cb964d80, c8efe4c0e9, c9518fe5b0, c9ab3fef06, ca2396b505, caf1b8cc0d, d42e0b7c00, dcc6539702, e899ddf536, eecf33834c, fb4ea87688, ffd6efc6b4`

**(E) "route not in openapi.yaml"** in `CLAUDE.md`/`IMPLEMENTATION_PLAN.md`/`PRD-*.md`** — every
hit is a progress-log entry or planning paragraph accurately describing a route **at the time it was
built** (`POST /scenarios/:id/reset-to-baseline`, `POST /scenarios/compare` — both real, both later
removed by an unrelated follow-up, Phase 3.2 Task 1, `c045548`) or a plan's shorthand design intent
(`POST /solve` → actually shipped as `POST /scenarios/:id/solve` per G3.1). Historical/planning
narrative, correctly describing what existed or was intended when written — not a live claim about
today's contract.
  - `13168c10ea, 4437457f1c, 7da41eab63, a3979e4649, b930279666, cc8e5c47f5`

**(F) `superseded` — shared headings across the 5 `.claude/agents/*.md` role files** — these are
intentionally parallel, template-following role definitions (`AGENTS.md` documents this design
explicitly); sharing section headings ("Your domain", "Core skills", "Coordination", "Escalate to
the lead") across roles is the template working as designed, not one file superseding another. None
of the 10 pairs actually duplicate content — each domain section names disjoint file paths.
  - `03e4d84dd7, 343ce3fe41, 3750fb9c04, 682aa8ab63, 6b5354aca0, a872fbbf94, bd71eec3ac, d4fa1c9c0f, d77f108acc, f9c0ba3bd2`

**(G) `docs/product-insights/POSTHOG-AUDIT.md` — `lib/posthog.ts`/`openapi.yaml` line refs flagged
missing** — the doc uses a repo-wide shorthand convention (paths relative to
`artifacts/api-server/src/`, matching every other reference in the same doc: `middlewares/auth.ts`,
`app.ts`, `index.ts`) rather than full paths. Content verified byte-accurate against the real files:
`lib/posthog.ts:3-4` (`POSTHOG_API_KEY`/`POSTHOG_HOST` env names), `:18` (the null-guard export),
`:21` (`enableExceptionAutocapture`), and `openapi.yaml:573-584` (`AuthUser`'s required `id` field)
all match real line-for-line.
  - `48e0e2fae7, 922216e1ce, 94c200dfd1, f7aa5a8ba0`

**(H) `docs/design-system/ui_kits/studio/README.md`** — evidence strings carry a trailing `\`
(`artifacts/studio/src\`), a markdown-escaped-backtick parsing artifact from the checker, not a real
path segment; `artifacts/studio/src` and `lib/chapters.ts` both exist.
  - `08406815c0, 7632b72920`

**(I) `docs/error-plans/README.md` / `docs/product-insights/README.md` — `aggregates.json`/`<date>.md`
flagged missing** — both READMEs explicitly document these as workflow-generated, not
pre-existing/committed files (`product-insights.yml` writes `aggregates.json` as "a transient build
artifact, not committed"). Correctly describing runtime behavior, not a stale claim of existence.
  - `029ecc3635, 20e3bf620e, 54b1b0486a, 85a502cf36`

**(J) `docs/superpowers/metrics/README.md` — `docs/superpowers/gates/<cause>.md`** — a template
path; a concrete instance (`docs/superpowers/gates/doc_drift.md`) exists, confirming the pattern.
  - `7faa592cc5`

**(K) `conflicting_instruction` — topical/textual proximity, not genuine opposition** — spot-checked
12+ pairs across every distinct file combination in this class (agent-file vs `CLAUDE.md`, plan-doc
vs `CLAUDE.md`, `README.md` vs `CLAUDE.md`, skill-file vs `CLAUDE.md`); every pair found was
consistent or directly reinforcing (e.g. `backend-engineer.md`'s "edit `openapi.yaml`, run orval
codegen" vs `CLAUDE.md`'s "edit `openapi.yaml`... commit spec + regenerated output together" — same
rule, not opposing ones). Several hits anchor to the same long `CLAUDE.md:135` excerpt (the R0.8
"75 commits" story) regardless of the citing file's actual topic (e.g. `IMPLEMENTATION_PLAN.md`'s
contract-first rules, Python dep install instructions) — the detector is matching on rough
similarity/length, not real semantic contradiction. No genuine conflict found anywhere in this class.
  - `12a35740b7, 17f5d1ea56, 1b3a58ed89, 1b429055ee, 3f255a0749, 4272e3a023, 429ed259b5, 42ddfd244c, 452b4ece16, 480a4cdb28, 57a9a1a147, 57e0ec36d2, 60c31f9321, 6bf62d90f8, 6ec7e604ec, 6f473083a2, 751f5f3630, 76793ad522, 77d432d322, 8070c32a56, 87bcca9b77, 8941548f9b, 8d6412dd8d, ab9c051e48, c37e7665b9, cab430f65f, cb78a3e005, d3c5270f5b, d9b8fc90ad, e11ca1a4cf, e770b47640, ea84d9ffec, f099113907, f34adef69b, f37abb5680`

## Carried over

None — no docs-audit PR was open when this sweep started (last week's PR, #4, was already merged).
