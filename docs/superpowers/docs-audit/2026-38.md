# docs-audit — week 2026-38

Sweep of the 175 mechanical candidates from `pnpm docs:audit --full` (55 in-scope docs). Every
candidate was verified against real code + git history (not against other docs). 2 real findings
(2 commits, one hunk each); 3 suppressed as within the 8-week dismissal window from last week's
sweep (2026-37); the remaining 170 dismissed below, grouped by root cause — most of this week's
noise traces to a handful of systematic gaps in the mechanical checker rather than 170 independent
judgment calls.

## Sweep 2026-09-19

### Finding b62d082226 · stale_reference · repo · high · replace · commit faedc7f
- **Where:** `.agents/memory/studio-stack.md` "Solver decision" section (L8).
- **Evidence:** claims "the solver is a pure TypeScript greedy + 1-opt local search" with
  "Python/PuLP unavailable (uv not in PATH)".
- **Rests on:** CLAUDE.md's own commit note `3b3fc1c feat: replace TS heuristic solver with
  PuLP/CBC ILP`, plus `artifacts/api-server/src/solver/solve.py` and
  `artifacts/api-server/src/solver/requirements.txt` (`pulp==3.3.2`) on disk today. The real solver
  is Python (PuLP + CBC), invoked from `jobRunner.ts` via `spawn`.
- **Proposal (applied):** replaced the passage with the correct solver description + a pointer to
  `e2e_accuracy.py` as the thing that actually exercises it.
- Folds finding `c8b41e439b` — same passage, same fix (evidence was the same "pure TS" claim from
  two angles: the false tech claim, and the false "PuLP unavailable" claim).
- **Also flagged in `.harness/docs-audit/candidates.json` as candidate origin for a prior,
  never-merged attempt at this same week's sweep** (branch `harness-weekly/2026-38-2`, PR #13,
  commit `93611ec` — same diagnosis, same fix, independently reproduced here). PR #13 is superseded
  by this PR (see PR body).

### Finding f7aa5a8ba0 · stale_reference · repo · high · replace · commit def8489
- **Where:** `docs/product-insights/POSTHOG-AUDIT.md` L27, "Identity equality" section.
- **Evidence:** cites `lib/api-spec/openapi.yaml:573-584` as the location of the `AuthUser` schema
  (`required id: string` field). That range is now the `/auth/user` GET operation body, not the
  schema — the spec grew since the audit doc was written and the citation drifted.
- **Rests on:** `grep -n "^    AuthUser:" lib/api-spec/openapi.yaml` → line 594; the schema block
  (`id`/`email`/`role`, `required: [id, email, role]`) runs 594-605, ending right before
  `AuthUserEnvelope:` at 606.
- **Proposal (applied):** updated the citation to `lib/api-spec/openapi.yaml:594-605`.

## Dismissed (grouped by root cause)

**A — glob/brace path syntax the checker doesn't resolve, but the base path exists on disk (23):**
`cb8dd79643 d2064bdaf8 8acd2d669f bf82ebec6f d455cafdda 7701b26724 73de31eec5 8343a6178b 7b491bc0b3
442671f614 6f54ae2ced 22b3131f8c e9afffc14d c3e21e811e 0137985b7b d417378022 725333fec0 1c588f46d3
c64a17321f 111f12b9ad 0ce6106865 6458079e54 ecfe1d671a`. Evidence is always `path/**` or
`path/{a,b,c}/**`-shaped; stripping the glob/brace suffix and checking the base (e.g.
`artifacts/api-server/src`, `lib/db`, `.github/workflows`) shows it exists every time. The checker's
path resolver treats the literal glob string as a path segment instead of expanding it.

**B — `<placeholder>`/`YYYY-WW`/`<date>` template tokens in prose, not real paths (11):**
`e2540748c6 79464fe679 0fb2ce8b2e 00e7b00eac 20a27fc8a1 20791f6e95 7faa592cc5 54b1b0486a 20e3bf620e
85a502cf36 029ecc3635`. E.g. `solvers/<model-id>/manifest.json`, `docs/superpowers/docs-audit/
YYYY-WW.md`, `docs/error-plans/<date>.md`, `docs/superpowers/gates/<cause>.md` — all describe a
family of real, per-instance paths (verified: `solvers/p-median-us/manifest.json` etc. exist;
`docs/error-plans/aggregates.json`/`docs/product-insights/aggregates.json` are documented
CI-workflow-generated artifacts, correctly described as not present in a checkout).

**C — bare `lib/X.ts` shorthand for `artifacts/studio/src/lib/X.ts` (3):** `ca27400a9a a5b45c2802
b9c2c36b1a` (`model-integration-precheck.md`'s `lib/mapBounds.ts`/`lib/entityId.ts`/
`lib/gazetteer.ts`). All three files exist at `artifacts/studio/src/lib/`; this doc consistently
uses the bare `lib/X.ts` shorthand elsewhere too (CLAUDE.md does the same for `lib/bands.ts`,
`lib/chapters.ts`, etc.) — an established convention, not a broken reference.

**D — bare `lib/posthog.ts` shorthand for `artifacts/api-server/src/lib/posthog.ts`, line numbers
verified correct (3):** `94c200dfd1 48e0e2fae7 922216e1ce`. `POSTHOG-AUDIT.md` consistently uses
paths relative to `artifacts/api-server/src/` throughout (`app.ts:63-65`, `index.ts:39,44`, etc.);
checked lines 3-4/18/21 of the real file against the doc's claims (env names, null-guard, autocapture
flag) — all accurate.

**E — trailing `\` is an escaped-backtick markdown artifact, not part of the path (2):**
`08406815c0 7632b72920`. Source is `` \`artifacts/studio/src\` `` / `` \`lib/chapters.ts\` `` — the
backslash escapes the closing backtick; the real referenced paths (`artifacts/studio/src`,
`lib/chapters.ts`) exist.

**F — `SendMessage` is a real Claude Code tool name, not a repo source symbol (6):** `6a226fc0b0
4198cdb0d9 1fced54826 07dbb35596 0b00dcf741 e57dad81a1`. The agent-team docs (`.claude/agents/*.md`,
`AGENTS.md`) document inter-agent coordination via the `SendMessage` tool — a platform tool, not
something that would ever appear via grep in `artifacts/`/`lib/`/`scripts/` source.

**G — `TaskCreate`/`TaskList`/`TaskUpdate`, genuinely ambiguous, not dismissed (3, carried over for
a human to confirm):** `7f2ddaa345 117589b546 c8e39bbc12` (`AGENTS.md:13`). Same shape as `F` above
(platform tool names, not repo symbols) — but unlike `SendMessage`, these three are not present in
this sweep's own tool list either. Tool availability can vary by session/harness config, which a
report-mode repo sweep can't authoritatively resolve — needs a human to confirm against the real
agent-team harness before treating this as either a false positive or a genuine doc bug.

**H — `pnpm update`/`pnpm audit` are built-in pnpm CLI subcommands, not custom `package.json`
scripts (2):** `f3b98bc13e e1eaf51ad7` (`devops-engineer.md`, describing the dependency-hygiene
backlog: "`qs`/`body-parser`... one `pnpm update` away from patched"; "no `pnpm audit`... gate exists
in CI"). Both are real, built-in pnpm subcommands usable without a `"scripts"` entry; the checker
only looks for custom script names.

**I — Arcadia/gamification mentions that correctly describe already-executed removal or explicitly
out-of-scope work, not current state (47):** CLAUDE.md `eecf33834c 926c6a77bc 5045dd84f5 e899ddf536
2910276b25` (Phase 1/A3 progress-log entries, "Remove gamification (backend/frontend)... Deleted
`pages/arcadia/`" — past tense, accurate); `IMPLEMENTATION_PLAN.md` `74c4dd3471 7155380d0f
2d5d4286c3 544e12ec02 ae2f4f7fb2 20f2c3faf5 c9518fe5b0 ca2396b505 b9146906a8 a59df5b383 21f8478991
b31903fdc4 bf84169565` and `PRD-network-optimization-studio-v2.md` `5c4ea68057 42e1b5fc97 c6308f971f
48d7062b7f 0fa59ed728 b1881aa109 c048ef36a2 ffd6efc6b4 af198d52ff 1e0c81df3a` (the original plan/PRD
task text for the de-gamification work that Phase 1/A3 already executed — root docs, in audit scope
per `docs-audit.config.json`, but a fixed historical task list by this repo's own convention: DoD
status lives in CLAUDE.md's progress log, not by editing the plan text after the fact — same
judgment last week's sweep already applied by leaving these two files untouched);
`application-audit-and-remediation-plan.md` `ac832f2bbf 1263138bf8 9aa527caa0 1749ec64d4 2742d9e590
6fdd45907d 8c083835b6 dcc6539702 fb4ea87688 c8efe4c0e9 8b3986f564 c7cb964d80` (an audit report
*about* the README's former Arcadia claims — historically accurate description of a documentation
bug, already fixed); `chapter-10-two-echelon-gold-refinery-implementation.md` `9aad117959
6b2b9fb6b8 78a557bb4f 274f60519f` and `-integration.md` `c9ab3fef06 9b6596865c caf1b8cc0d` (the
integration doc explicitly self-corrects: "An earlier draft of this plan included a task to add an
'Arcadia' quest... That subsystem does not exist in this repository" — already documents its own
staleness, matches CLAUDE.md's own note that M5 was "explicitly out of scope").

**J — same Arcadia/gamification evidence tag, but the cited line has no Arcadia/gamification text at
all (6, detector line-attribution bug):** `9e29497a46 823c116496 98500a77e1 b10b9a7266 07c33e80e9
a67c2145d5` (CLAUDE.md lines 192, 205, 212, 293, 296, 323 — checked directly, none mention
Arcadia/gamification/quest/badge/XP).

**K — "badge"/`Badge` is the shadcn UI component, not a gamification achievement badge (7):**
`82ba35f8df d42e0b7c00 5edd629ba9 89f28aea28 18b1f310c9 8902479de7 4517204d42`. E.g.
`docs/design-system/components/core/Badge.prompt.md`'s `<Badge variant="success">succeeded</Badge>`,
`docs/design-system/github.md`'s `components/ui/{button,badge,card,...}.tsx` — a real, currently-used
UI primitive (status pills like "stale"/"succeeded"), homonymous with the removed gamification
"badges" the Arcadia detector is really looking for.

**L — CLAUDE.md/IMPLEMENTATION_PLAN.md/PRD route mentions describing routes that existed at the time
and were later removed/redesigned, in past-tense progress-log or original-plan context (6):**
`7da41eab63 b930279666 13168c10ea 4437457f1c` (CLAUDE.md: `reset-to-baseline`/`compare` progress-log
entries for D6.1/F1.1, both later removed by Phase 3.2 Task 1 — documented in the same file at L266;
`POST /solve` inside a bug-narrative sentence describing the *old* buggy call path Round 2 fixed;
`POST /login` explicitly labelled "the legacy endpoint removed in Phase 1" two sentences later),
`cc8e5c47f5` (`IMPLEMENTATION_PLAN.md:197`, the original D6.1 task text — same "fixed historical
task list" judgment as cluster I), `a3979e4649` (`PRD:201`, describing 2026-07-era async-solve
design intent, superseded in practice by G3.1's real `POST /scenarios/:id/solve` shape).

**M — `.agents/memory/MEMORY.md` is the self-declared index of its own directory (1):** `3be87c38ac`.
"No inbound links" is expected for a root index file (by definition nothing points *into* an index —
it points out); it names and links all 3 sibling files in the directory. Not evidence of abandonment.

**N — agent-team role/skill files sharing generic scaffolding headings by deliberate template
design (10):** `f9c0ba3bd2 343ce3fe41 682aa8ab63 d77f108acc bd71eec3ac d4fa1c9c0f a872fbbf94
6b5354aca0 3750fb9c04 03e4d84dd7`. All five `.claude/agents/*.md` files intentionally follow the
same "Your domain / Core skills / [role-specific] / Coordination / Escalate to the lead" template
(confirmed via `grep -n "^##"` across all 5) — five parallel, currently-used, non-overlapping role
definitions, not one document obsoleting another. Same class already dismissed for
`docs-apply`/`harness-retro` SKILL.md last week (`docs/superpowers/docs-audit/2026-37.md`).

**O — `conflicting_instruction` detector matching topical/keyword proximity against CLAUDE.md's
narrative progress-log paragraphs, not real opposing directives (39):** `1b3a58ed89 1b429055ee
12a35740b7 cab430f65f 6ec7e604ec 76793ad522 f34adef69b 42ddfd244c 6f473083a2 fd847f53df 429ed259b5
d3c5270f5b ea84d9ffec cb78a3e005 3f255a0749 57e0ec36d2 57a9a1a147 d9b8fc90ad f37abb5680 77d432d322
17f5d1ea56 e770b47640 87bcca9b77 452b4ece16 8070c32a56 751f5f3630 8941548f9b 6bf62d90f8 ab9c051e48
480a4cdb28 4272e3a023 8d6412dd8d fe8ebd24b3 f0725c0952 9ab9aa293c d3c9b106c8 e11ca1a4cf 60c31f9321
f099113907`. Spot-checked ~10 across the full spread of "opposing" pairs (e.g. `devops-engineer.md`
vs `AGENTS.md:38`'s "Prove before done", `README.md:65` vs `CLAUDE.md:117`'s codegen rule,
`harness-retro/SKILL.md:3` vs `CLAUDE.md:44`'s branch-completion rule): every pair is either a
verbatim paraphrase of the same rule (agreeing, not opposing) or the CLAUDE.md side is one sentence
plucked from a multi-thousand-word narrative changelog paragraph that happens to share 3+ tokens with
an imperative sentence elsewhere. No genuine contradiction found in any sampled pair. The detector's
"opposing modals + shared tokens" heuristic has no real semantic-contradiction check, and CLAUDE.md's
`## v2 implementation progress` section (huge narrative prose, not instructions) is exactly the kind
of text that heuristic will false-positive against.

## Suppressed (dismissed within the last 8 weeks — 2026-37 sweep, not re-verified)

- `ab7341c93d` superseded `CLAUDE.md` — "superseded by" is prose about R0.8 history, not a
  file-supersede marker.
- `dd08b0fca9` superseded `IMPLEMENTATION_PLAN.md` — "superseded by Phase 3.5" is prose about
  `pmedian.ts`, not a document supersede.
- `83afba2394` superseded `.claude/skills/docs-apply/SKILL.md` — shares generic headings with
  `harness-retro/SKILL.md`; two distinct skills, neither supersedes the other (same root cause as
  cluster N above, already judged false last week).

## Carried over

None — every non-suppressed candidate from this run was resolved to either a fix, a dismissal, or
(cluster G) an explicit ambiguous/needs-human note this sweep.
